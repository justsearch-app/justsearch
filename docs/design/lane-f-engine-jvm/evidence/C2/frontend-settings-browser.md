# Frontend settings browser proof (2026-09-14)

The repaired harness captures all20 mapped Settings, Library and Brain states:
Settings1499 seven, Library1502 five, Brain1505 four unchanged ordinary states and
Brain1507 four corrected install drilldowns. All20 measurement companions contain
zero axe violations, zero console errors and no document overflow. Root inspected
the Simple search and open install-consent images. These are fixture-backed structural
checks; live backend/trust/model/installed verification and the broader lane campaign
remain required. This does not claim the full ui-check campaign passed.

The shared setup now uses the native Density range, Search v3's composer and explicit
Search this text action, its current result row and the header's Detail level buttons.
Mode setup waits for the witnessed COMPLETE fixture response. Each browser context
owns its settings fixture observation and keyed receipts, so subsequent reads see
committed values and retries do not mint another witness. CLI single-shot, affected
batch and ui-check failures preserve diagnostics and return nonzero. In particular,
the actual failed Brain1501/1503 batches now exit1, closing the earlier false success.

Brain install controls require Simple mode, a nonempty preview and an uninstalled
runtime. The dedicated install-preview variant supplies those facts only for the four
install drilldowns. It selects two fixture components with CPU sizes, labels and terms
from the real registry to exercise required/improves-results groups and consent. This
is fixture plan selection, not evidence that the live hardware planner ran. Its state
vocabulary and tier labels match the producer; other scenarios retain their defaults.

Final1508 passes218 Python UI/CLI tests and the UI-step coverage gate. Independent
review checked the CLI exits, scoped fixtures, current controls and preserved capture
evidence, and found no remaining scoped defect after correction. The adjacent manifest
records sources, per-image measurements, exact capture generations and accessible raw
paths. No production frontend code changed during harness repair; frontend1485's
6,573 tests remain applicable.

Preserved failures and review corrections:1466 had two valid Settings images and five
failed selectors despite exit0;1493 progressed past Density but still waited for old
result rows and nonexistent radio controls. The first CLI repair targeted ui-check;
root review required fixing the actual ui-shot single/affected branches too. Root
took the remaining diff after two worker correction rounds.1501 revealed hidden
Simple controls;1503 then revealed absent preview data and an online runtime whose
primary action shut AI down instead of opening consent.1505 captured all eight Brain
states, but independent review found the fixture's non-producer pending state was
being normalized by a compatibility fallback.1507 re-proves the four drilldowns with
to-download and actual tier labels. No failed capture or fallback pass is waived.

Raw artifacts remain in active tmp through lane acceptance plus30 days; export before
worktree release. Owned Vite helpers5185/5186 are closed through the identity-checking
repository sweep after captures; tmp/ui-proof-sweep1509.json records the outcome.
Remaining work continues with C2-7/C2-8 and the required integrated proof tiers.
