# User-requested pause, 2026-09-21

The user requested a clean stop during declared-survival admission verification.
No next implementation batch was started. A/B/C1 remain complete; C2 remains open,
and D1/D2/E/F remain. Merge placement is still F; draft PR727 is not merged.

## Saved implementation

The active worktree is `.claude/worktrees/lane-f-pr1-verify`, branch
`codex/lane-f-pr1`. Resume there, not the historical lane-F-A tree or main.
The governing mechanism and per-item split are in
[declared-survival-admission.md](declared-survival-admission.md).

- `acab91d5c`: registered INGEST/REINDEX refusal matrix, with generation guard
  negative controls; this is .3c.2a, not both-store restart proof.
- `3846e814a`: deferred consent audit publication, retaining atomic single use.
- `8aa3be11f`: runner key arbitration around lookup/reservation/raw acceptance,
  with publication outside the stripe.
- `705e60e8b`: explicit survival policy, catalog declarations and schema/projection.
- `30030ada5`: REST matched admission and native MCP parsed admission. Direct
  operations use one correctly classified slot, including at capacity one.
- `14f3dad74`: dispatcher/child handoff and live-context coordinator validation.
- `4796d8171`: launcher composition and actual-facade regression. These are
  implementation checkpoints with final review still owed.

The dispatcher reserves changed-survival child work before consent consumption and
acceptance. Matching recorded keys skip reservation. Accepted durable work detaches
when its caller goes away. Review corrections move approval preview outside the
runner stripe and pass live owner urgency plus the accepted authorization basis to
the handler. The row retains its acceptance snapshot. Coordinator validation permits
foreground-to-background only when the actual live durable owner confirms it.

The CLI/smoke launcher has no index owner. Its composition now omits INGEST/REINDEX
and dependent workflow projections instead of claiming nonexistent recovery owners.
The real Engine keeps those catalog entries during temporary runtime outages.

## Verification and limits

Evidence paths below are repository-relative, under this retained worktree's `tmp/`.
They are local artifacts, not hosted proof. Preserve them until C2 evidence has been
archived or replaced by accessible final-revision evidence; no automatic retention
deadline is claimed. Commands and complete output are in each named text log.

- `1952-live-urgency.txt`, `1952-xml`: 121 service cases and 36 coordinator cases
  pass, no skips/failures; associated PMD tasks pass. Includes legitimate detach
  and forged-background-label refusal.
- `1953-runner-arbitration.txt`, `1953-xml`: 20 runner/preparation/key lookup cases
  pass with both PMD tasks. Primitive review was clear.
- `1954-admission-regressions.txt`, `1954-xml`: 30/31 UI cases pass. One new test
  incorrectly compared the durable row's omitted process-local work ID with a live
  work ID. SqliteOperationStore.readContext intentionally reconstructs an unattached
  context. The correction asserts the row has no work ID, the handler has one,
  and attaching that ID resolves its actual durable owner; urgency and authorization
  assertions remain. It does not add process-local identity to persistent storage.
- `1956-closeout-focused.txt`, `1956-xml`: all 31 UI, 19 service and 47 launcher
  cases pass, no skips/failures. Includes same-key real admission/capsule race,
  cross-thread preview reentry, caller close before handler, and actual launcher
  composition. Aggregate failed PMD unnecessary qualifiers introduced by new imports.
- `1957-closeout-restored.txt`: qualifier cleanup briefly missed three assertion
  imports, causing launcher test compilation failure. Copied XML here is stale and
  is not credited as a new test run.
- `1958-closeout-restored.txt`, `1958-xml`: successful restoration. Launcher reruns
  47 cases; services reuse the unchanged successful 19-case test result. Both test
  PMD tasks pass. UI main/test and services main PMD passed in the prior focused runs.

These checks cover the pause snapshot, including uncommitted changes subsequently
saved in the item commits. They are not full-suite or final hosted verification.
The three independent review findings have implementation corrections and focused
regressions, but independent re-review of those corrections remains required.

## Resume in this order

1. Re-read current instructions and world state; verify branch and clean/pushed HEAD.
   Inspect origin/main and PR CI without confusing prior successful revisions with
   the pause snapshot. Keep commits per item and push each; retain hourly WIP rule.
2. Independently review the final handoff, coordinator live-context validation,
   preview placement and launcher projection. Audit cleanup/listener lifetimes,
   explicit DURABLE-to-INTERACTIVE overrides and cancellation barriers around
   acceptance. The same-key integration race is now covered; actual agent/workflow
   nested forwarding proof remains owed. Check how nested admission refusals map
   through API error handling; no claim of typed nested transport proof is made.
3. Update canonical operation/context documentation, regenerate derived docs and
   verify schema/route/governance projections. The design is current; canonical
   follow-through for this newest admission correction remains unfinished.
4. Run coherent full local suites, stress/architecture and required checks. Preserve
   XML before targeted reruns overwrite reports. Do not credit `1950-xml` or
   `1957-xml` to the runs named by those directories.
5. Rebuild the owned stack and prove actual DURABLE ingest/reindex rows, detach,
   receipts/acknowledgements and retries. Prior live1924 indexed five documents and
   live1925 answered a real standard-model query, but live1929 rows were INTERACTIVE;
   that evidence does not prove the corrected durable path or crash survival.
6. Finish .3c.2b: both SQLite stores close/reopen with new runner/coordinator,
   winning Resume bodies and old producer exit barrier. Then installed supervisor,
   process-death/index acknowledgement and final hosted proof. Complete remaining
   C2 acceptance reconciliation, then D1/D2/E/F under their owning designs.

## Stopped resources

No owned dev stack remains (last live run was already stopped with ports closed).
The final Gradle run completed. All delegated workers returned; no further work was
dispatched after the pause request. `1955-closeout-sweep.txt` reports another session's
ui-shot helper with uncertain ownership and the ownerless OTLP singleton; both were
left untouched. Worktree hold is recorded through review date 2026-09-28 because the
lane remains unfinished. Main's foreign dirty files were preserved.

Prior hosted CI35538297441 at3898195ab was cancelled with integration unfinished;
other jobs passed. Later pushes may supersede hosted runs. Final pause-head hosted
status is not reconciled and is not claimed green. Remote CI may continue after this
local pause. No owner decision is pending; resumption requires the user's next request.
