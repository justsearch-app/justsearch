# Lane F resumption and working contract, 2026-09-21

Resumed at the user's request from `28d5082cd`, clean and equal to
`origin/codex/lane-f-pr1`. The September 21 pause is historical. Continue in
`.claude/worktrees/lane-f-pr1-verify`; preserve main's unrelated dirty work.
A/B/C1/C2 are complete; D1/D2/E/F remain. Merge remains at F in
[draft PR727](https://github.com/justsearch-app/justsearch/pull/727).
Existing lane-scoped decision, checkpoint publication and merge authorization
continues. No routine owner decision is pending.

Current continuation: [D1 component plan](../D1/component-plan-2026-09-21.md)
and [initial owner wiring evidence](../D1/owner-wiring-2026-09-21.md).
The [hosted correction](hosted-governance-reconciliation.md),
[lock/replay correction](hostile-lock-acceptance.md), and
[bulk implementation](bulk-reindex-connection.md) are completed C2 history;
use [C2 acceptance](verification-2026-09-21.md) for current proof.
An overall-success workflow or job can conceal failed integration tests (including
ignoreFailures with BUILD SUCCESSFUL); inspect required proof at test level.
Do not treat a quiet log tail as a passing run:
check the exit code and preserved full XML, including earlier --continue failures.

## Working contract recovered from the prior session

The prior session and its user-supplied September 12 review were examined before
resuming implementation. These are operational lessons, not new product scope.

- Start from the named acceptance test and required proof tier. Complete the real
  consumer path before adding adjacent mechanisms or collecting adjacent proof.
  Commit counts and foundation tests do not measure stage completion.
- Use one independent review per coherent batch. Consolidate corrections; after
  two substantive rounds the root reassesses and owns the remaining diff. Do not
  repeatedly rediscover documented facts or turn every finding into a new mechanism.
  Required defects still need correction; this is no waiver of correctness.
- Keep one compiling commit per item, with a body naming scope, command and result;
  push checkpoints and preserve WIP at least hourly. Run focused checks while
  correcting, then full required suites once the batch is coherent. Re-run when
  changes invalidate proof, not merely because another checkpoint was written.
- Preserve output and XML before another run overwrites them. Commit a concise
  revision/command/result inventory; retain accessible raw artifacts. A copied
  report from another run, a hash alone, or configured CI is not executed proof.
- Bound evidence collectors as carefully as test processes. An orphaned PowerShell
  XML collector for hosted proof 2145 grew to 47 GB private memory and caused a
  later JUnit cleanup failure in full run 2168. Verify collector exit and native
  process identity; a task summary saying "no build active" does not prove all
  owned commands exited. Use bounded Python XML parsing and retain only counts or
  selected cases, not PowerShell XML DOMs across an artifact tree. Stop only a
  positively identified owned orphan; do not kill another lane's process.
- Isolate each negative-test cause. Mint wrong-key and wrong-nonce capsules after
  an expiry-clock advance so those cases cannot pass merely because all tokens
  expired. Prove consumed-token refusal using its original binding separately
  from copied-key refusal.
- Root owns shared lifecycle, Gradle, stack and integration. Delegate bounded
  independent analysis, tests or review with exact files and escalation criteria.
  Keep one Gradle run and no compiled-source edits during it.
- Continue after status answers and checkpoints. A user's pause stops dispatch
  and closes owned resources; it does not revoke the lane's decisions on resume.
- Keep current decisions in owning sections and a compact current handoff.
  Historical amendment logs and private transcripts are background, not required
  reading at every context reset. Change stale designs only with evidence,
  alternatives and the owning acceptance obligation recorded.
- Discover exact paths with `rg --files` before reading an unfamiliar owner; search
  directories with `rg -g` rather than passing shell globs as literal paths. Keep the
  resulting owner map instead of repeatedly guessing sibling filenames.
  Before sending a shell search, check that wildcard characters occur in `-g`
  filters rather than path arguments. On Windows use `python -X utf8` for text
  edits; the default cp1252 reader is not safe for canonical documents.
  Size combined source excerpts to fit the tool output budget; truncating several
  files in one call loses evidence and causes repeated reads. Search symbols first,
  then read the relevant bounded ranges. Updating this instruction is not proof of
  compliance: the resumed root repeated both path-glob and oversized-read mistakes
  during D1, so those efficiency lessons remain incompletely followed.
- Distinguish a fault fixture's prerequisite from a product contract. The replay
  investigation explained a real supersession schedule but did not justify a new
  durable claim table. Refute the proposed mechanism against intended latest-content
  behavior before expanding state. Isolate the selected producer and assert its
  exact child/revision; preserve separately proven committed-effect accounting.
- When an I/O correction fails under concurrency, test the existing behavior as a
  control before blaming the new mechanism. Windows2085 showed ordinary read handles
  already block atomic replacement. Preserve original red tests and compare alternatives
  at the smallest owning seam; do not replace atomic writes with a passing non-atomic move.
- Validate counter semantics before using them as acceptance totals. Recorded completed
  units count novel INDEXED receipts, not unchanged skips or total plan membership.
  Preserve all-root sealed/ACK evidence and exact corpus coverage rather than forcing
  a wrong count. Refute reviewer proposals with owner code before implementing them.
- After changing shared ledger semantics, search all direct ledger-count assertions in
  the affected module before selecting focused tests. Preserving obsolete committed
  effects as general history must not publish a replacement job hash or recorded coverage.
- Recovery tests must remove the ordinary live success path before claiming repair
  coverage. The bulk ACK paging test initially could succeed through live owner ACK;
  reject that ACK, retain the terminal row, then require inventory-only repair beyond
  a full contradictory page. Compare retained counters to captured prior projections,
  not guessed relationships such as revision minus one.

- Inspect native transaction ownership and the existing busy handler before adding
  application retries. Deferred SQLite read-to-write upgrades can bypass the native
  wait; raw BEGIN IMMEDIATE was selected only after independent visibility/rollback
  proof and inspection of JDBC's implicit next-transaction behavior.
- Discover ignored generated evidence with `rg --files --no-ignore` or
  `rg --no-ignore`. Ordinary repository search hides tmp/build logs and XML; absence
  from that search is not evidence that a runtime log was never retained.
- Check actual hosted test selection. `@EnabledOnOs(WINDOWS)` does not schedule a
  test in a Windows-only tag lane; both the task and its tag must be selected.
  Preserve local discovery proof separately from a later hosted passing result.

- Schema-version changes need a repository-wide inventory of direct version assertions,
  including neighboring admission/provenance/switch-buffer tests, before the first full
  module run. Update the expected current version while retaining all data-preservation
  and injected rollback predicates.

## Technical lessons that constrain continuation

### D1 continuation audit after2287

Retained strengths: root owns all builds/runtime lifecycle; bounded workers use
separate files and freeze before checks; test XML and exact source inventories are
preserved; failed compilation is not credited as proof. Captured-config regressions
now open the actual index runtime. Encoder composition still needs the separately
assigned real wiring regression; a fabricated successful observation was insufficient.

Remaining execution failures: oversized combined reads recurred after compaction,
and the current handoff accumulated a chronological C2 narrative. Root moved that
narrative below the historical boundary and restored a short current checkpoint.
Use bounded source ranges and `rg --max-columns` for design files with long lines.
The API field-count ceiling had been raised for a new handle; root restored30 by
making the constructor-only requested port local. Preserve the test's constraint
and simplify ownership before considering a constraint change.

Agent-system changes already shipped in AGENTS/CLAUDE cover path discovery,
continuation and bounded delegation. No new hook or global model setting is
justified by these lapses. Durable current-state repair is useful; adding another
instruction copy would not prove compliance. No user prompt ambiguity caused these
problems: autonomous continuation and full acceptance scope were explicit.

### Resumption self-audit, 2026-09-21

The user requested an explicit check against these lessons during the continuation.
Autonomous decisions, preserved acceptance scope, one shared build owner, real
consumer integration and truthful proof distinctions are being followed. Efficiency
is less consistent: repeated narrow log/code reads, guessed paths, incremental
feedback while a worker is still editing, and a large uncommitted connected batch
repeat earlier friction. Consolidate feedback after a frozen deliverable, discover
paths before reading, run each required proof at its coherent boundary, and update
the current evidence once per finished batch. Commit the connected bulk batch after
its required checks; do not let adjacent installed-fault work postpone that checkpoint.
Test totals are evidence, not a substitute for closing the named acceptance items.

At publication batch2258, bounded test delegation and two consolidated review rounds
found real deferred/swap/reload gaps; root retained lifecycle and build ownership.
All six negative controls failed for the intended predicates, with byte-exact restore
and preserved XML. Repeated guessed paths, a literal Windows glob, and one default
cp1252 edit still violated the documented workflow. The new AGENTS path-discovery
rule improves delivery, but is not evidence that the behavior is solved. Use the
verified owner map and UTF-8 edit commands on every continuation; do not add another
hook or representation merely to claim an agent-system improvement.

Successful ingestion/search does not establish declared survival or crash recovery.
Live1924/1925 proved ingestion and a standard-model query; live1929 exposed
INTERACTIVE rows. Re-prove the corrected DURABLE path before crediting recovery.
Use real composition to expose missing catalog declarations and recovery owners;
isolated coordinator fixtures did not expose those gaps. Distinguish the accepted
row's immutable context from live owner urgency and process-local work identity.
Reserve changed-survival work before consuming consent or accepting a row;
preserve the original frozen attribution and publish callbacks outside key locks.
The installed/restart tier remains required for recovery, not for every plumbing
correction.

## Current acceptance and next actions

The [current handoff](../../handoff.md) owns the revision, acceptance state, owner/evidence
map and next actions. The recovery, composition and historical verification below
explain earlier checkpoints; they are not a second current execution queue.

The user requested persistent agent-system improvements after the self-audit.
The canonical `docs/reference/contributing/agent-workflow.md` now specifies path
and evidence discovery/reuse, checkpointing a connected slice before adjacent work,
and updating current state in place. Existing delegation and verification rules
were already adequate and were not duplicated in AGENTS.md or another hook.
Both harness review/closeout skills were reviewed; their existing routes and
scope remain valid. This documentation is guidance, not proof of compliance.

The first resumed CI triage retained job logs in `tmp/1959-hosted-app-ui.txt`
and `tmp/1959-public-claims.txt`. The former fails
`PreparedOperationDispatchTest.crossClientRetryPreservesFrozenOriginWithoutBorrowingItsWorkId`;
the latter reports `operation-surface/undeclared-surface` for
`OperationWorkHandoff.java`. Local reproduction of the governance gate agrees.
These are failures, not waived historical noise. Local evidence is retained in this
worktree through lane acceptance plus 30 days and must be exported before release.

Read this record, the current handoff, and the owning admission/composition records
on context reset. Consult the [pause record](pause-2026-09-21.md) for exact saved
commits and qualified focused results; do not reread the entire historical handoff.

## Resumed admission correction evidence

`68ed2c2f0` registers the process-local handoff as a consumer of the existing runner
port. The previously failing operation-surface gate and the two neighboring gates
pass with zero findings; the enforcer's six regression checks pass.

Independent review found missing cancellation delivery for an explicitly interactive
child of durable work. The correction uses the existing handoff lock and checks
cancellation before handler entry. It adds no durable owner or new admission API.
The hosted retry assertion is updated to the selected reserve-before-consume order,
while preserving frozen-origin, handler authorization and persisted-row assertions.

| Run | Actual result |
| --- | --- |
| 1960 | Reproduces hosted retry failure locally: the mock expected admission after consent stamping, contrary to the selected order. Six unrelated guardrail cases pass. |
| 1961 | 37 executed cases across eight suites pass, no failures/skips; includes four reverse-survival regressions. UI integration compilation, PMD and format pass. |
| 1962 | Removing child cancellation registration makes the acceptance-barrier test fail because cancellation incorrectly returns while acceptance is blocked. Three guardrail cases pass. |
| 1963 | Removing the pre-handler cancellation guard makes the accepted-but-cancelled test fail because dispatch enters the handler instead of throwing cancellation. Three guardrail cases pass. |
| 1964 | Sources restored byte-for-byte. 37 cases are represented by unchanged UP-TO-DATE/FROM-CACHE results from1961; PMD/format pass. This is not a new execution of those tests. |

[Committed command/result and source inventory](resumption-admission-verification.json)
links retained raw logs/XML; hashes supplement those accessible artifacts.
Independent correction re-review found no remaining substantive defect in this
bounded cut. Real front-to-dispatcher/SQLite composition, agent/workflow forwarding,
explicit listener-registration cleanup proof, coherent full suites and the
live/restart/installed tiers remain open. Controlled front dispatchers and slot
counts alone do not prove those composition and listener claims.

## Actual front and caller composition checkpoint

The next bounded proof connects real REST aliases, generic invocation and native MCP
through the catalog, dispatcher, admission and SQLite. The prepared effect is inert:
this is ownership/composition proof, not indexing. Each direct front needs one slot,
retains its expected identity/source/transport and accepted FOREGROUND row, drops
live demand to BACKGROUND after the response, and releases the owner on completion.
AgentToolDispatcher and WorkflowShapeRunner cover nested callers with actual consent,
child work identity, parent detach and durable completion. AgentLoopService's existing
fresh/resume/fork tests separately prove workId survives session rebasing using its
scripted model and controlled dispatcher; this is split composition proof, not a
single model-backed end-to-end test. Explicit registration tests cover cleanup after
accepted completion and refusal for both direct and nested handoffs.

Focused1972 executes42 cases in13 suites, zero failures/errors/skips; format passes.
Independent review finds no remaining defect in this batch. Broad1971 build passes;
its30 represented integration cases include9 pre-existing reranker skips and reused
indexing/reranker results. It is not the full unit suite. Full1974 passes as recorded below.

Negative1970 removes the provenance projection and fails all five new composition
cases through the preparation consistency guard. It does not reproduce the original
silent INTERACTIVE acceptance. More discriminating negative1973 removes only the
shipped INGEST survival declaration: all five cases fail their DURABLE-versus-
INTERACTIVE assertion, while three guardrail cases pass. The catalog is restored
byte-for-byte before full1974. Logs/XML/counts are retained under tmp/1970*,
tmp/1971*, tmp/1972* and tmp/1973* in this worktree, with the same retention policy
as the earlier evidence. The initial1973 script rejected a newline mismatch before
editing any source or running Gradle; the corrected script produced the cited run.

Hosted CI35543728688 at `657b6b89a` passes app-ui and public-claims, confirming both
takeover failures are corrected on hosted runners. At the last inspection, every
reported job except the still-running system integration job passed. This is not a
completed all-green workflow claim. Raw snapshots: tmp/1977-hosted-runs.json,
tmp/1977-hosted-jobs.json and tmp/1978-hosted-status.json. The newer composition-test
diff is not part of that hosted revision.

The installed-scenario audit preserves all six kill points while correcting stale
outcomes in C2 section 7: unknown keys may execute once on their first retry; the
pre-successor ACCEPTED snapshot is distinct from post-boot recovery; and committed
settings use their exact witness rather than ingestion unit counters. The local
both-store reopen design is recorded in recorded-production-composition.md. These
are selected next steps, not executed proof.

Full1974 executes all34 test tasks:11,083 cases in1,744 suites, zero failures/errors,
30 skips; no test task reused prior results. The skips comprise19 asset-gated model
checks, six filesystem/platform checks, two external-fixture/collector checks, and
three explicitly deferred composition-discipline checks. They are not passed proof.
Detailed cases/reasons are retained in tmp/1974-skips.json; XML entries without a
reason were checked against the owning source assumptions. None is a newly added
composition test. Final1980 PMD for all three changed test modules and Java format
pass. Canonical links, generated index/skills, module graph and config matrix checks
pass. [Command, revision and source inventory](resumption-composition-verification.json)
links the complete retained logs/XML. Next: corrected live DURABLE ingestion/reindex,
then both-store reopen and installed recovery. C2 and the later stages remain open.

Owned stop2002 succeeds with portsClosed=true; post-stop2003 confirms ABSENT,
no foreign runs and no inference orphan. The browser and persistent MCP client are
closed. CI35543728688 was cancelled when the newer87195eac9 checkpoint was pushed;
its completed passing jobs remain partial hosted evidence, not final integration
proof. See [the live record](live-resumption-2026-09-21.md) for current next actions.


Current-session retention note (2026-09-21): use `followup_task` for a completed
subagent; `send_message` queues context but does not restart an idle agent. Check
status before assuming delegated follow-up is running. New integration fixtures
must stub the actual read-only rebuild-generation port separately from serving
capture, and distinguish full successful coverage from the intentionally bounded
failed/superseded history sample. Reuse the existing HeadAssembly operations
maintenance timer; locate the composed callback before proposing another scheduler.
