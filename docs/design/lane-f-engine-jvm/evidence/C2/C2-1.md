# C2-1: independent operations store and upgrade compatibility

Status: C2-1 WIP checkpoint, 2026-09-12 10:04UTC; implementation and integrated verification in progress. Base
`e247533bd`; all changes are root-owned in `worktree-lane-F-A`. No C2 stage closure
or durable-operation acceptance/replay claim is made by this foundation item.

## Implementation and decisions

- The app-api `OperationStore` port is injected explicitly into EngineRoot and
  HeadAssembly. HeadlessApp opens one SQLite implementation before the async fork;
  LauncherEnvironment owns its corresponding scoped instance. Neither half closes
  the borrowed store. Normal shutdown closes it after a confirmed index drain.
- Failed index drain retains both the store and the data-directory instance lock.
  A successful cleanup retry may close them; otherwise process termination owns
  final release. Startup cleanup waits at most five seconds for the producer;
  cancelling its future does not claim that its thread stopped.
- Schema1 in operations.db includes the agreed D1 fields and singleton recovery
  fence. Existing jobs.db becomes schema15 with nullable content_hash; existing
  owner, recoverability and reconciliation identities remain unchanged.
- Both stores inspect a private main+WAL copy before writable setup. SQLite mode=ro
  can write SHM, so opening originals could violate byte-preserving future-version
  refusal. Copying uses bounded temporary disk proportional to one DB+WAL; it is
  deliberately a quiescent-startup operation, not an online snapshot authority.
  This is simpler and safer than hand-writing WAL checksum/commit parsing.
- Quarantine uses one timestamped `.pending` directory, resumes every remaining
  artifact there and renames it complete before creating the replacement. A new
  directory per restart split the original main and sidecars, so the existing
  preservation-directory name now distinguishes incomplete work. No extra journal
  or marker file exists. The completed directory witnesses the expiry fence until
  replacement metadata commits. Health reports the preserved set and lost history.
- One migration transaction now owns both the version ladder and current-version
  privacy repair. DDL and user_version commit together. Unchecked failures roll
  back; an uncertain rollback closes the connection instead of enabling auto-commit.
- The updater permits incoming supersets while preserving every installed identity.
  `--compat-baseline` emits the prior release's set with current target formats.
  Source verification corrected the draft: old-shell intent expectations came
  from that descriptor, not the successor register. The installed shell validates
  each inherited expectation and expands to its full register before exact Engine
  request/response reconciliation. New stores cannot escape target checks.

## Evidence so far

All paths below are relative to this held worktree; raw artifacts remain accessible
through lane acceptance plus30 days. Review date2026-10-12 does not authorize deletion.

| Check | Result and evidence |
| --- | --- |
| Release projection negative/restored | 518 negative; 519 and539 pass12 tests |
| Updater successor negative/restored | 520 negative; 521 pass82 tests |
| Dependency locks | 522 resolveAndLockAll --write-locks passed; three generated locks |
| Initial operations store | 523 passed9 tests |
| Version-write atomicity | 524 negative with retained XML; 526 migration+queue passed |
| Wiring compile | 527 exposed incomplete call migrations; 528 passed |
| Store/reason governance | 529 and530 passed; 538 exposed unclassified temporary-copy writer; registered and543 passed75 self-test assertions |
| Unchecked rollback and WAL-only future schema | 531 reproduced2 failures, XML retained; 532 passed restored suites |
| Lifecycle/upgrade focused tests | 532 passed store, migration, queue, Engine lifetime, UI shutdown/reconciliation and launcher cleanup |
| Abrupt process death | 533 store suite passed16 cases, including6 JVM halt points; architecture negative fixture initially lacked interface metadata and was corrected to import its port explicitly |
| Target owner expansion | 535 reproduced3 failures; 536 full Rust suite passed85 tests |
| Release descriptor final | 539 passed12 tests |
| Documentation | 540 links passed;548 all8 regen sets passed; indexes and both installer skill projections reviewed |
| Current-schema privacy rollback failure | 546 reproduced failure with XML retained; restored proof pending integrated suite |
| Integrated build | 537/542/544/547/549/550/551 exposed formatting, PMD and remaining source-set call migrations; corrections retained;552 found a static-import edit error, corrected;553 is running and has one observed dead-code failure for constant-only OperationSchema |

Commands and full output use `tmp/c2-1-<subject>-<number>.txt`; negative XMLs are
`tmp/c2-1-version-atomicity-negative-524.xml`,
`tmp/c2-1-migration-review-negative-531.xml` and
`tmp/c2-1-privacy-rollback-negative-546.xml`. Focused XML snapshots are retained in
`tmp/c2-1-focused-evidence-545` before integrated tests overwrite their module reports.

## Remaining before batch1 closure

The independent correction review reports no remaining scoped correctness defects.
Run553 still requires a positive result: move schema creation into OperationSchema
so its runtime ownership is explicit, then rerun the failed boundary and integrated
checks. No gate exemption or suppression is authorized by this finding.

Finish build/unit verification; reconcile this
item's acceptance results. Then implement and run the day-one C2-11 installed
PROCESSING replay plus no-duplicate-effect harness as its own item commit. The
OperationStore port's acceptance/checkpoint/runner API and actual producer binding
are C2-2 onward; current crash proofs establish schema, preserved artifacts and the
persisted fence, not a yet-unimplemented key-expiry response. Every later C2/D1/D2/E/F
item remains authorized and required; merge placement stays F/PR1.

## Correction review outcome

The read-only reviewer checked the latest store/snapshot/migration, HeadlessApp,
EngineRoot, HeadAssembly, LauncherEnvironment and Rust updater sources and found no
remaining actionable defect in that bounded scope. It did not run tests. All review
fixes stayed with the root implementer; one consolidated correction assignment was
used. The latest migration and shutdown regressions still require positive execution
in553 or its corrected rerun; older XML must not stand in for those results.

Docs554 passes, agent parity555 passes8 checks, regen548 passes all8 generated sets.
The checkpoint is explicitly WIP and does not close C2-1 or batch1.
