# C2 prepared-handler resumption — 2026-09-20

User resumed the lane after the usage pause. Worktree lane-f-pr1-verify, branch
codex/lane-f-pr1, clean starting revision f51d9147e; fetched origin/main has no
commits missing from this branch. Existing autonomous authorization and merge at F remain.
The owning scope is [recorded-handler-connection.md](recorded-handler-connection.md).

## Hosted checkpoint correction

[CI34903071637](https://github.com/justsearch-app/justsearch/actions/runs/34903071637)
at f51d9147e passed all jobs except Build (no model blobs). Build failed at
app-agent:pmdTest with UnusedPrivateField on IngestToolTest.record and .context.
The recording fixture captured these but never asserted them. Added identity assertions
to replayUsesFrozenPlanAfterInputAndSuppliersChange, proving the accepted handle and
EngineContext reach the recorded service unchanged. No production behavior or check was relaxed.

Local1848 at f51d9147e plus the two assertions:
`./gradlew.bat -PtestParallelism=1 :modules:app-agent:test --tests '*IngestToolTest' :modules:app-agent:pmdTest :modules:app-agent:spotlessCheck`
passes: eight cases, zero failures/errors/skips; test and PMD execute.
Log tmp/1848.txt and copied XML tmp/1848-xml; hosted failure log tmp/1847-hosted-build-failure.txt.
Workflow routing report tmp/1847-workflow-health.md is repository-wide, not this head's verdict.
Evidence is retained in this worktree through lane completion plus 30 days, at least 2026-10-20.

## Prepared-handler completion checks

Independent source review found an explicit collection over 256 characters escaped as a
generic failure. The handler now returns BAD_REQUEST before reading preparation owners;
RecordedRootPlan and its replay projection share the existing limit. Boundary tests restore
invalid shapes/limits, reserved and blank collections, mixed labels, relative paths,
file/directory shape, frozen validation and bounded preview after deleting source files.
Real filesystem tests cover special-file refusal with a Unix-domain socket. Windows skips
symbolic-link creation (missing privilege) and unreadable POSIX permissions (unsupported view);
Linux hosted execution remains required for those two cases.

Removed only the superseded agent-adapter scan forwarding in HeadAssembly/factories and
its LocalApiServer publication. The REST controller's adapter still receives its registry
and rollup ledger. Renamed the factory suite to AgentToolFactoryCompositionTest, retaining
registration, adapter and journal identity checks; recorded-owner tests replace five obsolete
scan-binding cases. Canonical knowledge, storage and agent docs now describe the installed
prepared handlers and explicitly identify REST as still awaiting connection.

| Check | Actual result and retained evidence |
| --- | --- |
| 1849 snapshot negative control | Removing snapshot synchronization fails `snapshotCannotObserveRegistrationBeforeItsCollectionIsPublished` because membership escapes before the label. Source bytes restored. tmp/1849-negative.txt and -xml. |
| 1850 factory negative control | Replacing the live supplier with the original client fails eager and late cases of `everyConstructionPathUsesCurrentClientAndTheSameRecordedOwner`: generation-1 instead of generation-2. Source bytes restored. tmp/1850-negative.txt and -xml. |
| 1851 / 1853 initial integrated attempts | PMD exposed an unnecessary qualifier in the new boundary helper, then two existing WIP qualifiers in ValidatorRunnerTest. Corrected without suppression. Logs and pre-rerun XML are retained separately. |
| 1854 full affected suites | All four test tasks execute with includeStress=true: app-api 234 cases; app-agent 692; app-services 2,956; UI 1,246. Total 5,128 cases/725 suites, zero failures/errors, six skips (two platform limits above, four existing). All eight PMD tasks execute and pass. tmp/1854-full.txt, -counts.json and -xml. |
| 1855 boundary negative control | Permitting 257 characters and swapping file/directory classification fails exactly three selected cases: typed refusal and both frozen shapes. Source bytes restored. tmp/1855-negative.txt and -xml. |
| 1856 restored/architecture | Restored app-agent full suite reuses identical1854 cache; launcher tests execute 37 cases/16 suites without failures/errors/skips, including layering, index-writer ownership and unreferenced-method guard. Four module formatting checks pass. tmp/1856-architecture.txt, -counts.json and -xml. |
| Docs/governance | Index/skill regeneration and checks plus canonical links pass. Three gates (operation-surface, execution-surface, register-guard-resolution) pass with zero findings; tmp/1852-governance.txt. No generated skill delta. |

1854 command: `./gradlew.bat -PtestParallelism=1 -PincludeStress=true`, followed by
`:modules:<module>:test`, `:pmdMain`, `:pmdTest` for app-api, app-agent, app-services and ui;
format application on the first three modules and ui:spotlessCheck precede/ accompany the run.
1856 runs app-agent:test with the same properties, then app-launcher:test selecting
`*UnreferencedCodeTest.no_unreferenced_non_public_methods`, `*LayeringEnforcementTest`,
`*IndexWriterOwnershipTest`, plus the four affected module spotlessCheck tasks.
Tested source is 3f40791cb plus this committed handler correction/cleanup batch.

Hosted CI35532072555 at3f40791cb passed every job except build, which exposed the same
ValidatorRunnerTest qualifiers corrected above (tmp/1854-hosted-build-failure.txt).
CLA35532070785 passed. This does not verify the subsequent handler correction batch.

## Hosted prepared-handler checkpoint

[CI35533098123](https://github.com/justsearch-app/justsearch/actions/runs/35533098123)
and [CLA35533095933](https://github.com/justsearch-app/justsearch/actions/runs/35533095933)
pass at132b25ec1. Downloaded unit-test-attribution-app-ui into tmp/1858-hosted-app-ui.
Its IngestPreparationBoundaryTest XML records all11 cases executing on Linux with
zero skips/failures/errors, including symlink, unreadable-file and socket refusal.
This closes .3a's hosted filesystem proof; it does not verify subsequent .3b changes.

## Remaining work

REST connection (.3b), production integration (.3c), and all later
C2 live/model/installed acceptance remain required. No stage boundary or acceptance is waived.
