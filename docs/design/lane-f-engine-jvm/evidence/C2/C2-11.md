# C2-11: installed operation recovery evidence

Status: batch1 day-one harness implemented and installed test564 passed on2026-09-12. All six keyed
operation fault points remain required with C2-2 through C2-10; this foundation
fixture does not claim them.

The new OperationResumeE2ETest calls the existing installed Engine recovery owner.
Its operation scenario reuses the identity-verified PROCESSING crash, asserts the
row after actual Engine death and before restart, and waits for successor DONE and
searchability. It then retries the same file, requires a later queue timestamp and
DONE, and checks exactly one matching document and unchanged indexedDocuments.
The existing registered-run stop path must report portsClosed:true.

The extraction child starts with the existing blocking fixture and the successor
uses the normal extractor. No process-name sweep or new lifecycle driver is added.
The process identity collector's Windows limitation remains explicit. The passing
command is :modules:system-tests:integrationTest --tests '*OperationResumeE2ETest'
-PskipWebBuild=true --console=plain; installDist remains a task dependency.

Evidence below establishes the batch1 assertion; the complete six-point C2-11 item
remains open until the keyed runner and checkpoint owners land.


Independent read-only review found no actionable scoped defect. It verified the
real extraction hold, identity-checked kill and post-death row observation, the
strictly newer DONE timestamp after retry, exactly-one-document baseline, one
matching path hit, unchanged document count and retained identity-scoped cleanup.
The reviewer ran no tests; this is source review, not installed execution proof.


## Installed proof564

Windows11 / Temurin25.0.2 / Node24.12.0. The installed distribution was rebuilt
by the test task. Tested source:479a19dd7 plus the C2-1 corrections now committed
as396d16e22 and the C2-11 source files captured below. The run began before that
C2-1 commit was created; neither its code nor the harness changed during execution.

BUILD SUCCESSFUL in38s; the one JUnit case ran31.611s with zero failures/errors/skips.
The owned run408aa606-3ab9-401b-a674-0c7bb4289692 restarted once into incarnation2,
new instance58f4d6c1-c09c-4edf-8ba6-9e9cbc54afb3. The row was still PROCESSING after
actual death (last_updated1789208891460), then DONE1789208909336. Retrying accepted
one job and produced a later DONE1789208915961; indexedDocuments and matching path
hits both stayed exactly1. The registered stop reported portsClosed:true,
errors:[], and gracefulBackendShutdown.outcome:exited. The already-exited frontend
was observed as such in stop-report pidLiveness; its taskkill128 is not a surviving
process or an ignored fixture error. Subsequent quick_health found ABSENT with no
foreign run or inference orphan.

Accessible raw evidence (relative to the held lane worktree):

- tmp/c2-11-installed-564.txt — full Gradle output.
- tmp/c2-11-installed-564.xml — retained JUnit XML; SHA256
  50d97fbb1273d27bfa4f0229270283c5677f876c4e39fdef8c5e5b7fb74d9232.
- tmp/lane-f-takeover/writer-junit-fb1e596b-112a-4a25-94bd-8a12bc485a3b/fixture-output.txt
  — PROCESSING_AFTER_DEATH, PROCESSING_REPLAY_PASS and OPERATION_RETRY_NO_DUPLICATES_PASS.
- That fixture directory's state/runs/408aa606-3ab9-401b-a674-0c7bb4289692/stop-report.json
  and incarnations/ contain the lifecycle, shutdown and Engine evidence.

Retention: lane acceptance plus30 days; export before releasing this worktree.
Batch1's integrated build565 is still required before closure. This proves existing
PROCESSING replay and path-idempotent retry, not future same-key acceptance or the
three keyed fault points for write and reconfigure.
