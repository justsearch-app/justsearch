# Complete prepared-target presentation — September 13

C2-3 UI follow-on at9eacf1099 plus this item. AuthorizationHost wraps long target
segments and preserves whitespace without truncation. Border-box sizing and a viewport
height bound keep the complete dialog scrollable with reachable controls. The typed
confirmation input now has its visible instruction as an accessible label and fits the
content box. The broker's display contract names both raw summaries and frozen targets.

The [executable witness](verify-prepared-preview-ui.py) composes jseval's existing
isolated-step, worktree serving, structural fixtures and measurement facilities without
editing its shared runner. It loads the actual app broker module and exercises the real
Lit host. No backend mutation is approved. Registered --affected lookup has no generic
step for this file, so this scoped executable supplies its runnable geometry regression;
the existing UI coverage gate also passes.

-998 is harness setup failure only: SES rejects dynamic import in evaluated code. The
witness uses normal module loading without weakening lockdown. -999 then captures four
real failing cases: a552px dialog has3220px horizontal content for420-byte targets and
67316px for8192-byte targets, at both1568px and840px viewports.

1000 is another harness setup failure, not rendering proof: after a broker source edit,
Vite's HMR module URL differs from a bare import and creates a second broker singleton.
The witness now resolves the app graph's observed broker resource URL and requires
exactly one match. 1002 proves horizontal wrapping but exposes a906px dialog in a900px
viewport, leaving maximum-size approval controls unreachable. The height bound fixes
that in1003. Measurements also identify an existing missing typed-input label; the
visible instruction now labels it, clearing the axe finding in1004.

Final-mechanism negative1006 removes wrapping/height bounds from the otherwise corrected
source. All four cases fail horizontal containment; actual labels, typed input and
approval controls still work, ruling out a setup failure. After exact restoration,
**1007 passes all four cases**: full420/8192-byte text at1568/840px,510px dialog content
width with no horizontal overflow, viewport-bounded height (maximum868px), approval
enabled after typing and reachable by scrolling, zero axe violations and console errors.
The script refuses missing/failed axe measurement rather than accepting an empty report.
Root read the facts and visually inspected the840px representative screenshot.

Full frontend1008 passes6480 tests across483 files, zero failures/pending/todo; typecheck,
focused ESLint and UI coverage pass. The unit log retains non-failing happy-dom teardown
abort output. [Verification JSON](prepared-preview-ui-verification.json) records commands,
source hashes and raw witness/PNG/measure/log locations. Keep artifacts until acceptance
plus30days and export before worktree release. This is structural UI proof with fixtures,
not a live prepared producer/API/model/installed witness. Agent/workflow continuations
and integrated C2-3 acceptance remain next. Merge stays F/PR1.
