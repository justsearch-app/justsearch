# Retained-base settings callers (2026-09-14)

C2-6 item3c migrates Library exclusions and legacy contrast reconciliation.
All eight frontend POST sites now use the shared attempt helper. This closes
caller implementation, not C2-6 acceptance: the UI harness repair, no-bypass
retirement and integrated governance/live/model/installed/final-head proof remain.

Library keeps the witness read with its loaded exclusions. It cannot edit/save
after a failed or unwitnessed load. A conflict retains the draft and requires an
explicit Reload saved patterns before another logical attempt. Remount preserves
that draft instead of silently loading over it. COMPLETE advances the local witness;
Preview/Apply run only afterward. One busy flag covers save/effect overlap, and an
uncertain timeout retains the frozen attempt for the next check. Editing/reload
cannot replace that pending attempt's base. These are view-local draft fields,
not another persisted revision, queue or operation store.

Legacy contrast uses the witness from the same canonical read. Open receipts replay
the exact request; failures preserve the old marker. Only COMPLETE (or an already
equal canonical observation) permits clearing. Profile and legacy-value guards
prevent a later profile/value from being cleared or projected by the older request.
The conditional document mutation repeats that check. Appearance retains its
existing single DOM writer.

Independent Library review caught an unbounded GET: aborting an ignored signal did
not release the new busy state. Root extracted readSettingsObservation from the
existing helper's bounded/validated read, then reused it for Library, migration and
absolute intents. Promise.race bounds even an uncooperative transport, and a late
result cannot overwrite a newer observation. No second deadline implementation or
cache was introduced. The timeout message now says only that the request timed out;
GET does not imply an accepted mutation. Typed write errors retain attempt identity.

Final1485 passes489 suites and6,573 tests, zero failures/skips. Typecheck and focused
ESLint pass at the same source. Focused1484 passes60 cases (28 helper,18 migration,
8 Library,6 Brain mode). Negative1476 restores unconditional Apply-after-failure and
fails the forbidden core.apply-excludes call. Negative1481 removes the same-profile
value guard and fails both the pre-admission no-POST and postcompletion DOM checks.
Restored tests pass. Adaptation received an independent clear review; Library's
deadline/shared-read correction also received an independent clear review. The reviewer
inspected source and regressions but did not execute the commands; root owns the raw proof.

Preserved failures:1471/1472's remount spy included an unrelated ingestion-summary
GET, then was narrowed to the settings endpoint;1474 caught the root test transport's
HostFetchInit/RequestInit mismatch;1478 exposed a single-microtask test assumption;
1482/1483 caught the old exact timeout message (including an incomplete replacement).
These were fixture corrections, not waived contracts. Full1480 already passed6,571
cases before the two independent value-drift cases. Existing connection-refused and
happy-dom teardown-abort stderr remains visible; no live backend proof is inferred.

Exact commands, hashes and accessible raw paths are in the adjacent manifest. Keep
active tmp evidence through lane acceptance plus30 days and export it before
worktree release. UI capture1466 remains partial: two valid Settings images with
zero axe/console errors, five failed downstream selectors. Root owns repair/reproof.
Merge remains at F; no owner decision is pending.
