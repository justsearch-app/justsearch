# C1 governance sweep

The routing sandbox is now a registered logic seam, with its measured100% strength and zero
uncovered-mutant ceiling. The executor consult region uses the existing path-based register,
covering module edits and the resource register. The source census floor is65 explicit factory
specifications and80 logical registrations after helper expansion. These are current measured
floors, rather than the review's older64/79 count.

The jobs-db recovery row's currentVersion is checked against TARGET_VERSION in its declared
versionAuthority source. The check uses the existing lexical scanner to strip comments and
literals, requires exactly one positive decimal declaration, and rejects missing or unreadable
authority. Other Java numeric bases fail closed. The register remains14; no duplicate
version constant or new schema field is introduced.

Routing tests now assert the returned artifact and forwarded policy and exercise both close
owners under success, runtime failure, fatal failure and shared-exception identity. The complete
mutation run also exposed a missing existing PassageWindows property: per-source considered
counts must be nonnegative and sum to the pre-admission total, including when admission drops
windows. That property now kills the line187 addition-to-subtraction mutant. Retrieval behavior
and existing strength floors are unchanged.

## Verification

Windows11, Temurin25.0.2, Node24.12; base4136d57e1 plus this item.

- Routing PIT350 initially showed13 mutants:4 killed,1 survived and8 uncovered. Strengthened
  tests352 kill all13, including returned-artifact and close behavior. Final complete PIT360
  measures all17 registered seams, routing13/13 and passage-windows59/71 effective kills (83%).
  PIT counts timed-out mutants as kills by the existing declared metric. The specific per-source
  counter mutant is KILLED by PassageWindowsPropertyTest, not merely timed out.
- Focused359 passes the routing10, property5 and executor architecture4 cases. Full build362
  passes in6s with test Error Prone enabled. This is compile/static proof, not a full test run.
- Node363 passes73 store-check assertions, the real44-authority recovery gate, all25 consult
  rows/67 recipes, and structural validation of17 logic seams.
- Old verifier353 fails all three new version-test groups. Actual register drift358 from14 to13
  fails against source14; the register is restored byte-for-byte. Routing structural check354
  fails without a baseline row and restored355 passes. Census probes356 at65/80 both pass,
  exposing the older review counts; forced logical floor100000 in357 fails and reports actual
  explicit65, helper expansion20, logical80. The final test uses65/80.
- Complete efficacy gate361 still fails: hybrid-fusion has10 uncovered mutants against ceiling9,
  and retired Worker supervision/liveness baseline rows have no current registered measurement.
  The inherited hybrid default-bound map overload now has normal-result and null/blank/invalid
  field assertions in its existing property test. Focused365 passes14 cases and PIT reduces
  uncovered mutants to6 (ceiling9 unchanged). The two Worker rows are retired with a classified
  seam-retraction changeset, completing A11/A10's already-authorized code/register deletions.
  Final366 combines fresh adapters365 with unchanged modules from complete360 and passes the
  actual efficacy gate in blocking mode:17 measured seams, zero failing findings. The two notes
  suggest stricter strength floors; they are not failures. Final build367 passes in6s.
- Independent read-only review found two parser defects: octal017 could be misread as decimal17,
  and an escaped triple quote could expose example code inside a Java text block. Adverse368
  reproduces both plus failure to distinguish the real declaration after such a block. The shared
  scanner now recognizes text-block escapes and optionally strips literals; its write-discovery
  default still preserves mode-bearing strings. Restored369 passes75 assertions and the real44
  authority gate. Reviewed build370 passes in4s. No mutation-targeted Java source changed after365.
  The same reviewer independently reread both fixes and reran those Node checks, finding no
  remaining defect in the original sweep scope. The later hybrid/baseline cleanup was separately
  source-traced by a read-only explorer and reread by root against fresh PIT and the blocking gate.

Raw logs and reports: `tmp/c1-routing-pit-350.txt`, `tmp/c1-routing-pit-results-350/`,
`tmp/c1-governance-node-351.txt`, `tmp/c1-routing-pit-352.txt`,
`tmp/c1-routing-pit-results-352/`, `tmp/c1-governance-results-352/`,
`tmp/c1-store-version-adverse-353.txt`, `tmp/c1-routing-before-pin-354.txt`,
`tmp/c1-routing-pinned-355.txt`, `tmp/c1-executor-census-adverse-356-explicit.txt`,
`tmp/c1-executor-census-adverse-356-logical.txt`, `tmp/c1-census-results-356-explicit/`,
`tmp/c1-census-results-356-logical/`, `tmp/c1-executor-census-adverse-357.txt`,
`tmp/c1-census-results-357/`, `tmp/c1-store-version-drift-358.txt`,
`tmp/c1-governance-focused-359.txt`, `tmp/c1-governance-results-359/`,
`tmp/c1-governance-pit-360.txt`, `tmp/c1-governance-pit-results-360/`,
`tmp/c1-pit-strength-report-360.json`, `tmp/c1-governance-efficacy-361.txt`,
`tmp/c1-governance-efficacy-361.sarif`, `tmp/c1-governance-build-362.txt`,
`tmp/c1-governance-restored-363.txt`, `tmp/c1-governance-hybrid-365.txt`,
`tmp/c1-governance-hybrid-results-365/`, `tmp/c1-governance-hybrid-pit-results-365/`,
`tmp/c1-pit-strength-report-366.json`, `tmp/c1-governance-efficacy-366.txt`,
`tmp/c1-governance-efficacy-366.sarif`, `tmp/c1-governance-final-build-367.txt`,
`tmp/c1-governance-lexical-adverse-368.txt`, `tmp/c1-governance-lexical-restored-369.txt`,
`tmp/c1-governance-reviewed-build-370.txt`, `tmp/c1-governance-docs-371.txt`.
The source overlay and input hashes for this tested item are preserved in
`tmp/c1-governance-source-overlay-371.patch` and `tmp/c1-governance-source-provenance-371.json`.
