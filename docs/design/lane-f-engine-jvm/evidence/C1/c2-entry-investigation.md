# C2 entry investigation before the user-requested pause

Read-only investigation at8f8c7d775,2026-09-09; C2 implementation has not started.
- C2.md I11 still says indexing withContext bound view. C1 actually uses required EngineContext parameters; re-ground every carrier/current signature before starting.
- OperationHistoryStore in app-observability is still a200-entry ring with listener fan-in outside its lock. Its module has no SQLite dependency. Decide the JDBC owner using owning/nearby patterns before adding a second store façade. The durable swap must keep this surface and listeners.
- OperationDispatcher now carries required EngineContext on dispatch/undo overloads, still no client key. The shared OperationExecutorImpl boundary is preferable to controller-only recording because agent/MCP callers also dispatch.
- C2-1 is separate operations.db (MIXED, HEAD, identity unchanged for existing stores) plus jobs content_hash V14->V15, additive-store updater rule and release-descriptor baseline mode. Defaults still need final decision:100000 rows,3 attempts are recommendations, not implemented values.
- Retention has a design gap to decide autonomously: arbitrary UUID4 keys plus bounded deleted history cannot distinguish an aged-out key from a never-accepted key. Do not implement unbounded tombstones or return false unknown. Investigate timestamp-bearing UUID keys/window qualification, clock-skew and cap/restore behavior; record a dated section0 decision before code. The design requires a client-supplied UUID but does not fix its version. No production UUIDv7 generator found; generated MCP runtime only contains Zod validators. Current web OperationClient merely forwards optional idempotencyKey, with test string uuid-key.
- C2-11 day-one harness is explicitly non-vacuous PROCESSING replay/no-duplicate proof, before adding three fault-point assertions with each owning item. Section7 permits that sequencing; no disabled test placeholders.
- Full pipeline470 initially had nerEnabled false while deferred models initialized; at186s the same run's readiness began requiring ner_not_complete. KnowledgeServer.java1667 publishes enabled from usable non-null model services, then releases modelReadyLatch. Do not claim NER completion from earlier skipped-stage snapshots; require final real completion on the resumed proof.

No owner question is pending. User delegates all decisions. C1 still needs a complete offline full-enrichment run and closure evidence; all other current C1 live, full/local/hosted proofs pass.

Further source notes:
- JobBatchExtractor254/267 already computes sourceSha256 before/after extraction and returns it in ExtractedJob299; JobBatchWriter129 forwards it to IndexingDocumentOps236 (SchemaFields.SOURCE_SHA256). C2 should project those verified bytes into jobs.content_hash rather than introduce another hash pass.
- The existing backup-restore reference is KnowledgeServer.handleCorruptDatabase2801ff: quarantine DB/WAL, copy .bak when present, then reopen. Reverify call ownership before copying this pattern; operations.db must never rebuild from jobs or silently erase accepted evidence. Its preserving policy needs a known restore transition that advances history_since. Arbitrary valid-file rollback cannot be inferred from only the rolled-back database; do not claim it can.
- OperationExecutorImpl317 canonical dispatch validates EngineContext/provenance then trust/capabilities before effects; acceptance must follow those validations and C1 admission. emitHistory416 honors auditSuppressed: durable acceptance must not be skipped by presentation-audit suppression.
- app-observability/build.gradle.kts currently has no SQLite dependency; existing job SQL lives in indexer-worker. New operations store's module/lifecycle must be decided explicitly and keep the existing OperationHistoryStore projection/listener surface.
