# C1 autonomous resumption, 2026-09-12

The user authorized integrating current main and resuming the remaining lane.
Merge9762cf593 incorporates #721 (agent roles and usage accounting) and #722
(worktree lifecycle) without conflicts, and is pushed. Application sources and
model inputs are unchanged from8f8c7d775; the three intervening lane commits
are C2/D1/inference-host design changes, not implementation or stage proof.

## Integration checks

Windows11, Temurin25.0.2, Node24.12. The full stress-enabled command at9762cf593
passes in49s,3 executed/366 up-to-date tasks. Represented XML:9783 tests
(25 skipped),118 integration (52 skipped),50 schema,1 load-sensitive and9 system,
zero failures/errors. These are reused unchanged-input reports, not a claim that
all cases re-executed. The later evidence snapshot includes only the advisory
baseline shrink as an overlay. Raw: `tmp/c1-resume-integrated-478.txt`,
`tmp/c1-resume-integrated-results-478/manifest.json`.

Agent parity passes all eight checks. The current session cannot instantiate
the newly configured companion role (unknown agent_type); an explorer carries
the bounded read-only context brief. Configured10 is an anomaly guard, not proof
of the active session's capacity. Additional focused tooling checks and their correction are recorded below.

The current advisory producer and unchanged gate pass. Shrink-only rebalance
removes the two high identities imported from main; runtime-client has zero
advisories, ui-web only lower severities. Existing smol-toml1.7.2 is preserved.
Raw: `tmp/c1-resume-advisories-479.txt`, `tmp/c1-resume-advisory-report-479.json`,
`tmp/c1-resume-advisory-gate-480.txt`, `tmp/c1-resume-advisory-gate-480.sarif`.

## Evidence retention and live proof

The lane is registered to session01a082dc-dfd6-7d60-be2e-a8d088229a67 and held
through review date2026-10-12 for active implementation and mandatory evidence.
No release/removal is authorized while this proof remains the evidence source;
export required raw artifacts before eventual release because #722's disposable
evaluation paths do not satisfy this lane's retention obligation.

Fresh chat-offline pipeline483 runs under continuous search on owned stack
3cfef22b-fcb2-438c-b721-987fd431f106, API52282, data481, lease7200s, source9762cf593,
installed stamp061beab1388633ee. The attached MCP preflight still expects the
retired Worker distribution; the lane's documented CLI runs the current
registered Engine runner with the same shared ownership machinery.
First walk completed08:27:51.955804200Z and was captured08:28:28.865821Z, before
the60s periodic rescan. Full enrichment remains unproven while the run is active.
Raw: `tmp/c1-resume-offline-start-481.txt`, `tmp/c1-resume-first-walk-484.json`.

Next: finish483, preserve its final summary/timeline and owned logs, verify
current hosted CI, then reconcile every C1 acceptance row before C2 code starts.
C2 design amendments must settle same-key input conflicts, keyed undo, durable
history replay, and the settings commit/rebuild boundary. D1 must explicitly
bind runtime/encoders per query, correct its batch dependencies and retire the
previous-generation reference before pruning. None is owner-gated or omitted.

## Hook import correction and completed focused checks

Tooling482 was interrupted after importing worktree-base-hint left the Node test
child reading stdin indefinitely. The helper's documented import contract says
its command does not run on import, but the footer unconditionally invoked main.
The shared runHook(import.meta.url, main) direct-invocation guard already used
by its registration/release siblings is the correction; no new lifecycle helper
is introduced. Regression486 fails against the old footer (reads1 versus0).
Restored487 passes all four hook/ledger files;490 passes all11 files,162 inner
checks in31.69s, including actual temporary-worktree lifecycle/removal tests.
Standalone removal491 passes5 checks, including expected refusal output. Script
lint493, agent parity488 and consult recipe validation489 pass.

Hook-integrity492 initially fails because this worktree's ignored local Claude
settings lack the two newly merged hook bindings. Canonical regeneration494
updates only that local hook projection (tracked projections unchanged);495
passes with zero findings. Existing local permissions/env remain preserved.
Raw: `tmp/c1-resume-tooling-482.txt`, `tmp/c1-base-hook-negative-486.txt`,
`tmp/c1-base-hook-restored-487.txt`, `tmp/c1-resume-parity-488.txt`,
`tmp/c1-resume-consult-489.txt`, `tmp/c1-resume-tooling-restored-490.txt`,
`tmp/c1-resume-removal-491.txt`, `tmp/c1-resume-hook-gate-492.txt`,
`tmp/c1-resume-hook-gate-negative-492.sarif`, `tmp/c1-resume-lint-493.txt`,
`tmp/c1-resume-hook-regeneration-494.txt`, `tmp/c1-resume-hook-gate-restored-495.txt`.

These checks are atc9f5e3e93 plus the two hook-source/test files; Engine inputs
are unchanged, and the active483 process remains the previously installed binary.
