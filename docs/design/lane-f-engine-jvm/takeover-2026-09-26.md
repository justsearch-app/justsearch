# Lane F takeover — 2026-09-26

This is the entry document for the next implementing agent. It records the
handoff boundary; it does not replace the [locked design](design.md),
[D1 acceptance](stages/D1.md), [D2 acceptance](stages/D2.md), or the
[evidence ledger](handoff.md). Read those owning sections for exact clauses.
The predecessor transcript is not needed.

## Workspace and authority

- Worktree: `F:/justsearch-public/.claude/worktrees/lane-f-pr1-verify`;
  branch: `codex/lane-f-pr1`; draft [PR727](https://github.com/justsearch-app/justsearch/pull/727).
  Keep main and other sessions' work untouched. This worktree is held for Lane F.
- Existing checkpoint commits and pushes to PR727 are authorized. Merge remains
  conditional on stage F acceptance. Merge `origin/main` at each checkpoint;
  the 2026-09-26 pre-handoff merge found it already up to date.
- Own routine technical, test-intent and in-scope design choices. The owner
  explicitly retired routine permission requests. Use independent refutation
  for a genuine ambiguity and record the reason. Ask the owner only for a
  product choice, extra scope/cost, or an irreversible/outward action beyond
  existing authorization. No initial Astra assignment is pending.
- Root owns the single Gradle build and shared dev-stack lifecycle. Check its
  lease before starting it. Do not stage or remove the unrelated untracked
  `modules/app-inference/logs/` directory. Raw `tmp/` evidence stays in this
  worktree through lane acceptance plus 30 days.

## Current checkpoint and proof

`38d3bbda6` is the successor-witness design and fixture correction;
`b065f15a9` is the last fully hosted runtime revision. Resolve the actual
branch and remote heads before continuing.
[CI 36145039780](https://github.com/justsearch-app/justsearch/actions/runs/36145039780)
completed **success** on `b065f15a9`, including Windows-native and system
integration. The successor-witness/harness-only change in `38d3bbda6` has
focused and installed local proof, but no hosted run yet.

C2 is accepted. D1 remains in progress; D2, E and F remain open. The table
below is implemented and verified work within that incomplete scope.

| Implemented slice | Exact usable evidence and limit |
| --- | --- |
| D1-8/D1-9 live A→B publication, registered no-file source replay, source-gap decision and precommit cancel | Real Engine and installed standard-model rounds, including `tmp/3655`, `tmp/3663`, `tmp/3685`–`tmp/3686`, `tmp/3696`, `tmp/3699`. These cover their stated cuts only; D1-9 is not accepted. |
| Mid-replay process death; pointer-before and pointer-after process deaths | Separate installed JVMs recovered exact B and reopened VECTOR search: `tmp/3710`–`tmp/3711` and `tmp/3731`–`tmp/3734`. Scoped no-file journal rows were drained. |
| Citation readiness regression found by installed pointer cuts | A composed scorer with a null optional consumer model path formerly threw during diagnostic hashing. The focused test was green, deliberately red on the old code, then green (`tmp/3725`–`tmp/3727`). The installed cuts above reached both barriers after the fix. Consumer diagnostic logging is **not** the selected citation model identity witness; D1-13 still owns that map. |
| Latest integrated source at `b065f15a9` | `tmp/3735-pointer-cuts-integrated.txt`: serial stress, Spotless, PMD, test and installDist, 358 tasks, 11,991 XML tests, zero failures/errors, 33 skipped. `tmp/3736-pointer-cuts-compile.txt`: explicit `build -x test` passed. Hosted CI is linked above. |
| Successor-witness design correction at `38d3bbda6` | Independent review found the C2 successor `INGEST` row and `SUCCESSOR_ROW_MISSING` branch were never implemented. Dated §17.6 amendment uses the accepted reindex row, exact reopened writable B, sealed queue and settled replay. Writer/replay negative tests passed at `tmp/3737` (two methods, three invocations); recorded-boot binding and terminal-history tests passed at `tmp/3738` (three tests). The reviewer corrected three draft overclaims. `superseded_from` remains only as legacy v1 table-copy compatibility storage. |
| Retained predecessor capacity | The installed pointer-after JVM intentionally halted with B committed and A retained (`tmp/3741`). A separate JVM observed `startFreshMigration` refuse the third candidate without moving the pointer, then recovered B and answered VECTOR 10 (`tmp/3742`). Fixture compilation is `tmp/3739`. The new before-pointer diagnostic branch was **not** rerun after this fixture addition. |
| Owner improvement and gate changes | WP1 transition barrier, WP2 2a register/version-source check, WP3 order and WP5 docs are incorporated; the escalation-policy branch is merged and the two requested instruction checks passed. D1-16 has a harness skeleton with six explicitly pending feature scenarios, not six accepted scenarios. |

The installer-generation producer correction is in the branch: acquisition
reports “Downloaded — activation required,” and a distinct approved
HIGH/DURABLE/REINDEX operation freezes the candidate settings/model witness.
Ordinary settings still refuses generation-bound paths. Six installed
pointer/settings cuts are recorded at `tmp/3580`–`tmp/3585`. The owner-requested
mixed-model and drain defects were reproduced and corrected; see the
[installer decision](evidence/D1/generation-native-cursor-design-2026-09-23.md#7-installer-produced-generation-candidate-2026-09-23-amendment)
and the evidence ledger for their exact revision and limits.

## Next bounded work

1. Give `38d3bbda6` plus this handoff a hosted checkpoint and check it by
   exact SHA. Do not treat local focused proof as hosted proof. Review
   the before-pointer fixture branch or rerun it before claiming that new
   diagnostic. Keep pushes spaced so system integration finishes.
2. Finish D1-9/D1-8 Flow A through the D1-16 lifecycle harness: accepted
   writes during final replay and abandonment, remaining post-publication and
   cleanup cuts, exact candidate retirement, and semantic availability after
   refusal. The installed gap round proves A remains VECTOR-searchable, but
   does not measure the reconfigure-budget interval. Retained-capacity proof
   covers the post-pointer predecessor; name any unproved branch. The existing
   no-file fixture uses NRT. Pull forward only the minimal D2-5 identity and
   projection seam needed for D1-9; durable covering-commit and acknowledged
   delete remain D2-5 acceptance.
3. Continue the [WP3 order](continuation-brief.md#adopted-remaining-work-order-2026-09-25):
   D1-14/D1-13 backfill, floor-cap and native leases; D1-12 Flow B and WP4
   containment; D1-4 connected reconfigure; interleave D1-6/D1-7/D1-15 and
   D1-2/D1-10 proof; D1-17 sweep. Then complete D2, remaining WP2 release
   safety, E and F. No D1/D2 item is waived by the stage E re-cut.

Stage E has **seven paired branch/main groups**: search quality/workflow;
search and agent response time during bulk indexing; indexing speed; memory
plus owner-duration no-crash soak; crash recovery without orphans; graceful
and forced hang; dead-Engine upgrade. Other design §16 rows are one-sided
D1/D2 feature acceptance. Minimum-spec, other-OS recovery, representative
changes and collector comparison are conditional on their claims; use G1
unless response-time evidence fails. [E](stages/E.md) owns the runbook. Decide
whether the signed dead-Engine round is a hard merge blocker under WP2 2d; if
main-only signing prevents that proof, ask the owner for a one-off signed build
instead of silently carrying the gap.

## Verification and safety at resumption

Run `node scripts/agent-analytics/world-state.mjs`, verify this worktree's
branch/status and PR head, and read [continuation brief](continuation-brief.md),
the recent top of [handoff](handoff.md), D1 §12 and the owning acceptance item.
Check [CI](https://github.com/justsearch-app/justsearch/actions/runs/36145039780)
by exact SHA before reusing it. Only one Gradle build and one shared stack may
run. Before an integrated gate, run the touched schema generators,
`check-store-recoverability`, runtime-manifest closure when relevant,
`regen-all --check`, and Spotless/PMD. Preserve red XML and explain
wrong-reason passes. Local logs above exist in `tmp/` now; export them before
releasing the worktree. The owner requested a handoff, so do not infer a
feature-scope cut or permission to merge.
