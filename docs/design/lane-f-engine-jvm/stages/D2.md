---
title: "Lane F stage D2 — self-description and request-time paths: implementation checklist"
stage: D2
created: 2026-09-09
base: be47faa40
status: "draft, code-verified at the stage-B head; re-ground every citation at stage start (C1, C2 and D1 move the code first)"
updated: 2026-09-09
---

# Lane F stage D2 — self-description and request-time paths: implementation checklist

Written from 17.3 row D2, `verified-facts.md`, and a code-verified read at `be47faa40` (facts
gathered 2026-09-09 by a read-only pass; `file:line` at that commit; `design.md` cited by
section). C1, C2 and D1 land first and move this code; 17.6's stage-start re-grounding applies.

Stage D2 in one line (17.3 row D2): **the component map endpoint and its coverage test, the
composition profiles (`full`, `bench`, `verification`) and the Engine as a library (10); the
session gate with aging under the `passed` rule and the one-producer test (4, review seven);
index-and-return at the port with its durability test; the cursor generation token, reader
pinning for a live cursor under the retained-state caps, and `cursor expired` (4, 8); the
ephemeral store axis completed for the `verification` profile.**

---

## 0. Corrections to inherited facts (apply to `verified-facts.md` at stage start)

1. **The three "slice" surfaces disagree between the design and the port register, and none is
   a component list.** Design 10 names `/api/debug/state`, `/infra/capabilities` and
   `RegistrySnapshotExporter`; `governance/engine-ports.v1.json:99-103` (the `health` port) names
   `/api/health`, `/api/debug/state`, `/infra/capabilities`. At HEAD: `/api/health` is
   `LifecycleSnapshotV1` (`StatusRoutes.java:52`, `StatusLifecycleHandler.java:1137`);
   `/api/debug/state` (`DebugRoutes.java:28`, `DebugStateController.buildDebugState` `:91`) has
   keys `system, config, worker, gpl, reranking`; `/infra/capabilities` (`InfraRoutes.java:47`,
   `CapabilitiesService.CapabilitiesView` `:291-296`) has `schema_versions, prompt_templates,
   plugins, source, serverCapabilities`; `/api/runtime/manifest` (`RuntimeApiRoutes.java:70`)
   is the manifest; **`RegistrySnapshotExporter` is not an endpoint** — a build-time JSON export
   (`registry/snapshot/RegistrySnapshotExporter.java:48-50`) read at runtime only by
   `WitnessController` (`/api/registry/witness`). No `ComponentMap` symbol or coverage test
   exists. D2-1 builds the endpoint new and makes the four runtime surfaces projections; the
   exporter becomes a projection of the same registry snapshot.
2. **No composition profile exists; the library shape half exists.** Zero `profile` hits in
   `modules/app-engine`; the word's other uses are `search.pipeline.profile`, `chat.profile`
   (`compact`/`standard`), `vlm.profile` and jseval's bench-suite profile — unrelated. But
   `EngineRoot` already separates **embedded** (`EngineRoot(long deadlineMs, int batchSize)`
   `:84`, "no process-exit authority" `:77-79`) from **process** (`forProcess` `:89,95,103`,
   sole caller `HeadlessApp.java:1439`); the embedded form is used only by tests, and
   `EngineTestHarness` (`:42`) is package-private. `app-engine`'s public API is eight types.
3. **There is no session gate; there is one unfair semaphore, and one producer thread.**
   `NativeSessionHandle.java:116` `new Semaphore(1)` (single-arg, unfair, as 4 assumes);
   `acquire()` `:271-299`, `releaseGpu()` `:326-361`. No `passed`, `sessionGate` or aging symbol.
   Batch bounds today: embed 8 (`OnnxEmbeddingEncoder.java:291`), BGE-M3 4/2
   (`BgeM3Encoder.java:51-52`), SPLADE `getMaxBatchSize()` (`:311`), NER 16
   (`BertNerInference.java:291`). The one-producer claim is **stronger than the design states**:
   `BackfillScheduler.runIdleCycle()` (`:188`) runs synchronously on the `indexing-loop` thread
   (`IndexingLoop.java:572,669,731`), so there is one producer thread, not two coordinated ones.
   `SearchPerSourceExecutor` (`:42-43`) is an unbounded virtual-thread fan-out with a 10 s per
   future `get`.
4. **The submit surface is `KnowledgeClient.submitBatch`, and the sequence number is discarded at
   the Lucene call, not at the port.** `IndexingService` has no submit method; `KnowledgeClient`
   (`:100`, `submitBatch` `:488,499,511-524`) is the surface. `BatchResponse`
   (`indexing.proto:667-670`) carries `accepted_count` and `error_message` only. The `long`
   Lucene returns is dropped at `WritePathOps.java:89,98-103,124-131` and `CommitOps.java:110`
   (`commit()` returns elapsed ms, `:83,107-109`); the only reads of a seqNo are the NRT
   watermark (`NrtReopenStats.java:72,85`, `SearcherBridge.java:64`). No `durable`, `awaitCommit`
   or group-commit concept exists. The durability precedent is `SwitchBufferDurabilityTest`
   (`:30`, doc `:27-28`: "callers must check the return value and NOT acknowledge").
5. **Reader lifetime is per call, and the cursor code that exists is the wrong one.**
   `SearcherBridge.withSearcher` `:141-156` acquires at `:149` and releases in `finally`;
   `searchAfter` is at `ReadPathOps.java:426` (not `:343`), decoded per call (`:71,421`);
   `SearcherLifetimeManager` appears only in design prose. `errors.CURSOR_INVALID`
   (`errors.en.properties:43`, "The search cursor expired") names a malformed cursor, not an
   evicted generation; `cursor expired` as a distinct code is absent, and the retained-state
   register is C1-12's.
6. **Four file-path-only SQLite stores, not three:** `SqliteJobQueue` (`:135,245`),
   `SqliteDocumentIdentityStore` (`:29,50`), `SqlitePathResolutionStore` (`:62,68`) and
   `EntityClusterStore` (`worker-core/.../disambiguation/EntityClusterStore.java:39,48`).
   `IndexSchema.ephemeral()` (`:101`, `indexPath == null` → `ComponentsFactory.java:80,91`
   temp directory; delete on close `RuntimeSession.java:765-774`) has **no production caller**.
   No verification-profile boot-time number exists anywhere in `evidence/`.
7. **17.9's "`AutoCloseable` inference surface and `releaseGpu`" conflates two types:**
   `InferenceSurface` (`indexer-worker/.../server/InferenceSurface.java:39,50-58`, best-effort
   close over its handles) and `SessionHandle.releaseGpu` (`ort-common/.../SessionHandle.java:95`,
   impl `NativeSessionHandle.java:326`). `InferenceLifecycleManager.close()` is now conditional
   on the shutdown reason (`:1356,1368,1379-1390`, B5); the `verified-facts.md` bullet asserting
   an unconditional stop is stale.
8. **The generic MCP-client recovery harness (7.6, lock) has no code and no owner stage.** The
   design says "C2 or D2"; C2's checklist did not take it. D2-9 takes it.

### 0.1 Corrections found while implementing (appended per item)

*(empty at stage start.)*

---

## 1. Inheritance ledger

**2026-09-09 C1 batch-4 inheritance amendment.** C1 already introduced scan-progress retention
and an expiry response, persisted CANCELLED terminals, and an agent-history reconciliation timer.
These are preliminary changes, not acceptance of this stage. C2 must reconcile cancellation and
history against durable operation acceptance/effect/completion; D2 must reconcile scan expiry and
discovery against its cursor/component-map contracts. Review or retire the preliminary paths when
the owning item lands; do not create a competing state store or claim those obligations complete.


| # | inherited state | item |
|---|---|---|
| I1 | **C1**: `EngineContext` on both ports with the `withContext` view; the executor registry (C1-5) and `governance/retained-state.v1.json` (C1-12) with `cursors per context / aggregate` and `pinned readers` declared `awaitingProducer: D2`; the admission front and its codes. | D2-1, D2-6 |
| I2 | **C2**: the operations table and the outcome query by client key (what the MCP harness exercises); the ordering invariant. | D2-9 |
| I3 | **D1**: the recomposable registry (D1-1) with applied/attempted versions, the readiness envelope as authority with the four composites (D1-12), live activation (D1-7) which must retire pinned readers per 7.4's cursor rule, and the config-apply register. The component map is the endpoint over D1-1's registry plus C1's two registers. | D2-1, D2-6 |
| I4 | **The embedded `EngineRoot` shape** (§0.2) as the library seed; `HeadlessApp` as the only process composition. | D2-2, D2-3 |
| I5 | **The unfair semaphore and the one producer thread** (§0.3). | D2-4 |
| I6 | **`KnowledgeClient.submitBatch` and the dropped Lucene sequence numbers** (§0.4). | D2-5 |
| I7 | **Per-call searchers, stateless `searchAfter`, `CURSOR_INVALID`** (§0.5). | D2-6 |
| I8 | **Four SQLite stores on file paths; `IndexSchema.ephemeral()` unused in production** (§0.6). | D2-7 |
| I9 | **`encoder-latency-probe.sh` records no `ms` for `query-understanding` and `dense-retrieval`** (`evidence/baseline/README.md`), which E's request-time-encoders row needs. | D2-8 |

---

## 2. Parameters fixed before the stage (17.7)

| parameter | value | source |
|---|---|---|
| session-gate aging threshold | **first cut: 3 foreground seatings or 2 s wall clock, whichever first**; final at E | 17.7 (D2, E) |
| background batch size bound | the encoders' existing maxima (8 / 4 / SPLADE's / 16) declared in the gate, not raised | 17.7; §0.3 |
| group-commit coalescing deadline for `durable` | **first cut: 250 ms** | 17.7 (implementer) |
| cursor expiry | **first cut: 5 minutes idle, or eviction by cap** | 17.7 (implementer) |
| cursors per context / aggregate, pinned readers | C1-12's register values; D2 gives them producers | C1; confirmed at E |
| verification-profile boot time | the number E records; the design's "seconds" is the claim D2's test pins (**under 5 s** on the reference machine, first cut) | design 10; E |

---

## 3. Checklist items

### D2-1 — the component map endpoint and its coverage test

Design 10 ("The Engine describes itself"). One new endpoint, `GET /api/engine/components`,
paginated and field-filterable, listing **every component the composition root wires**: the
recomposable components (D1-1) with lifecycle state, desired versus applied config and the
winning tier, last error, evidence pointer; every registered executor (C1-5) with its kind,
bounds and current depth; every retained-state kind (C1-12) with its cap and count; the
memory lines (C1-13); the readiness composites (D1-12). The four existing surfaces become
projections: `/api/debug/state`'s `worker`/`system` slices and `/infra/capabilities` read from
the same snapshot type; `RegistrySnapshotExporter` exports that snapshot; `/api/health` is
D1's projection already. Registered in `governance/engine-ports.v1.json` (the `health` row
corrected to name the map), the OpenAPI/route regen set and the contract-surface register.

**Acceptance:** a coverage test asserts the endpoint lists every component `EngineRoot` wires,
every executor the registry holds and every retained-state kind, with a vacuous-pass floor equal
to the three registries' counts (a list that is a hand-maintained enumeration fails it);
a test asserts a filter on `state=failed` returns exactly the failed components; `regen-all --check`
green; `--gate engine-port` green with the row corrected; the ui-web API explorer reads it
(`run-ui-web-gates` green).

### D2-2 — composition profiles: stores × inference, three named

Design 10. `EngineProfile(stores ∈ {real, ephemeral}, inference ∈ {full, encoders, none})` with
the named combinations `full`, `bench` (real stores, encoders, no LLM), `verification`
(ephemeral, none). `EngineRoot`'s embedded constructor (§0.2) takes the profile; the process
composition passes `full`; the dev-runner gains `--profile verification`. `ephemeral` stores
mean `IndexSchema.ephemeral()` (its first production caller) and the temp-and-delete variants
of D2-7; `none` inference composes no ORT session and no generative backend, with the readiness
composites reporting `absent` (not `failed`, 7.6).

**Acceptance:** a test boots `verification` and asserts `api` and `index` ready with `encoders`
and `generative` `absent`, no file left under the data directory after close, and **boot time
under the §2 bound** on the reference machine (the number recorded in the test's log line for
E); a test boots `bench` and asserts encoders composed and no llama-server child; `full`
unchanged (`EngineRootInProcessPortsTest` green). The dev-runner's `--profile` is pinned by a
`test-dev-runner-*.mjs` case discovered by B9's runner.

### D2-3 — the Engine as a library, and the root kept a wiring module

Design 10 ("Engine as a library"; "a size ratchet on `app-engine` and the module fan-in report").
A public `EngineComposition` facade in `app-engine` (the package-private `EngineTestHarness`
made public and named) returning the ports (`KnowledgeClient`, the operations port, the
component map snapshot) for a profile; consumers landed in-lane: jseval's in-process eval path
(`bench` profile) and the CLI indexing entry in `app-launcher`. A size ratchet on `app-engine`
(lines of production Java, baseline recorded, never grows without a recorded reason) and a
module fan-in report (which modules depend on `app-engine`: must stay `ui`, `app-launcher`,
`system-tests`) as a test.

**Acceptance:** the two consumers have tests that compose through the facade without a process
(`SummarizationPipelineE2ETest` and `VduBatchProcessorE2ETest` already use the embedded root —
migrate them to the facade); the ratchet test reds on a growth without a baseline bump (verify
the bite, revert); `LayeringEnforcementTest` green.

### D2-4 — the two-level session gate with aging, and the one-producer test

Design 4 (the request-time encoders paragraph as amended by review seven). Replace the unfair
semaphore in `NativeSessionHandle` (`:116`) with a gate: a foreground acquire (urgency from the
context, C1) goes ahead of any waiting background submit; background batch sizes are bounded by
the declared maxima; a background submit that has waited past the aging threshold (§2) is seated
next, once, and **every foreground call waiting at that moment is marked `passed`; aging is
suspended while any `passed` call is still waiting**; a foreground call is overtaken by at most
one aged batch in its whole wait. The gate's counters (seatings, passes, aged batches) are on the
component map. The **one-producer test** pins that exactly one thread produces background
batches per stage: a test starts a second producer and asserts the gate refuses it with a reason
(the gate takes a producer token issued once per stage), so a future second producer fails
loudly rather than breaking the one-batch bound silently.

**Acceptance (the unit clauses of 16's request-time-encoders row):** one background batch in
flight per session; foreground-first seating between aging periods; exactly one aged batch
seated per period under a saturating foreground load; with a foreground queue deeper than twice
the aging threshold, no foreground call is passed by more than one aged batch over its whole wait
and aging resumes once the passed calls are served — each as a deterministic test over a fake
session with controlled timing (no wall-clock sleeps); the producer-token test; the indexing
throughput test under a saturating foreground load shows background progress of one batch per
aging period plus one admission cap's drain (the compatibility clause). *A test that asserts
"foreground goes first" and nothing about aging is not the acceptance; the aging bound is the
whole item.*

### D2-5 — index-and-return at the port, with its durability test

**2026-09-12, 955-6: owed by lane F.** Add identity-based delete-and-acknowledge
beside indexAndReturn, with explicit EngineContext. A durable deletion acknowledgement
follows the commit covering that delete, not queue admission or an NRT refresh. An
already-absent identity is idempotent but still uses an authoritative durable absence
proof. During reindex, D1 journals the delete before acceptance can be acknowledged
and replay prevents candidate resurrection. Failed commit returns failure; timeout
or cancellation leaves an observable open/failed attempt, never a durable success.
Register955's memory projection/note un-designation consumer in engine-ports and
prove immediate forced restart leaves the identity absent, concurrent deletions
coalesce commits, and reindex activation cannot restore an acknowledged deletion.


Design 4 ("Immediate index-and-return"); 16's durability row. A new port method on
`IndexingService` with C1's explicit context: `IndexAndReturn indexAndReturn(Document
doc, Durability d, EngineContext context)` where `Durability ∈ {nrt, durable}`; returns the document identity and
`visibility` (`nrt` or `durable`; and, during an in-place transition, `text-only, semantic at
activation`, D1-8's field). `nrt` returns after the write is NRT-visible; `durable` waits for the
next group commit, **coalesced within the §2 deadline** (never one fsync per call). To wait on a
commit the Lucene sequence numbers are plumbed: `WritePathOps` returns the `long` it discards
today (`:89,98-103,124-131`), `CommitOps.commit()` returns the max completed sequence number
beside the elapsed time (`:83,107-110`), and the coordinator exposes "commit covering seqNo N
done". Port-level only in lane F (no HTTP or MCP exposure, 6); the in-lane consumer is the
durability test, and agents-write-back (11) stands on it later.

**Acceptance (16's durability row):** a `durable` write is searchable after an immediate forced
kill and restart (the installed-process tier, B17's fixture shape); an `nrt` write is searchable
before the next commit and **may** be absent after the kill (asserted as the adverse case, so the
two durabilities are shown different); the coalescing test asserts N concurrent `durable` calls
inside one deadline produce one commit; `SwitchBufferDurabilityTest`'s rule applies: a failed
commit makes `durable` return failure, never `durable` success.

### D2-6 — the cursor generation token, reader pinning, and `cursor expired`

Design 4 ("Streaming through contracts"), 7.4 (cursors), 8 (retained state). A search page
cursor carries the generation id and a searcher token; the core pins a reader per live cursor
through a per-generation `SearcherLifetimeManager` (Lucene's primitive) in `RuntimeSession`,
released on cursor expiry (§2) or eviction; the caps come from `governance/retained-state.v1.json`
(cursors per context and aggregate, pinned readers; C1-12's `awaitingProducer: D2` rows get
their producers here, and the coverage test's kinds become live). Eviction is oldest-first per
context; an evicted or generation-retired cursor answers the **new** code `CURSOR_EXPIRED`
(distinct from `CURSOR_INVALID`, which stays for a malformed cursor; the message at
`errors.en.properties:43` is corrected to say "invalid"). D1-7's activation retires the old
generation's pinned readers per 7.4: served to normal expiry if the old encoder is still loaded,
`CURSOR_EXPIRED` if it is not, never silently served from the new generation. `searchAfter`
keeps its stateless decode (`ReadPathOps.java:421-426`) but resolves against the pinned searcher
when the cursor carries a token.

**Acceptance (16's aggregate-bound cursor clause and the generation row's cursor clause):** a
client paging one request at a time against a changing index sees its oldest cursor evicted with
`CURSOR_EXPIRED` once the per-context cap is reached; the pinned-reader count on the component
map never exceeds its cap; a cursor opened before activation pages to expiry (encoder loaded) or
fails `CURSOR_EXPIRED` (encoder unloaded) and never returns new-generation evidence — asserted
by a document that exists only in the new generation; memory holds through the run (E). The
readiness code parity tests green with the new code; `run-ui-web-gates` green with the FE
handling (a page that expires re-searches, no error toast).

### D2-7 — the ephemeral store axis completed

Design 17.3 row D2. The four SQLite stores (§0.6) gain a temp-and-delete construction (a
`StoreLocation` that is a path or `ephemeral`, deleting its file on close), used by the
`verification` profile; `IndexSchema.ephemeral()` stays as the Lucene half. The
store-recoverability register gains nothing (ephemeral instances are not durable stores; the
row's `ownedPaths` do not cover a temp directory) and `check-runtime-manifest-closure` is
unaffected (nothing under `runtime/`).

**Acceptance:** a test boots `verification`, writes through each of the four stores, closes, and
asserts no file remains; `StoreCatalogTest` green; `check-store-recoverability` green.

### D2-8 — request-time encoder timings in the trace

E's request-time-encoders row needs per-call query NER and query embedding latency, which
`searchTrace.stages` does not carry (`query-understanding` and `dense-retrieval` have no `ms`;
`evidence/baseline/README.md`). Add the two stage timings to the trace (the `SearchTrace` is the
canonical record; `governance/execution-surfaces.v1.json` registers its referencers, so the
`execution-surface` gate must see the change) and to `encoder-latency-probe.sh`'s summary.

**Acceptance:** `--gate execution-surface` green; a test asserts both stages carry `ms` on a
hybrid query; `encoder-latency-probe.sh` on the dev stack prints p50/p95 for both stages.

### D2-9 — the generic MCP-client recovery harness

Design 7.6 (the tested client set, lock) and 16's recovery-workflow row. A Node script under
`scripts/supervisor-conformance/` (beside B17's scenarios, run by the installed-process tier):
initialise an MCP session against `/mcp`, run a search, force-kill the Engine (identity-verified,
B17's helper), wait for `restarting` → `running`, re-run the bootstrap route, re-initialise the
session under the protocol version the MCP reference documents, complete a search, and query
the outcome of a write issued before the kill by its client key (C2). It tests the protocol,
never a host; the README names no host (18).

**Acceptance:** the scenario passes in `:modules:system-tests:integrationTest` (a new
`McpClientRecoveryE2ETest` case) with every step's log line; the same script is what E's
recovery-workflow row runs for the "generic MCP client" entry.

### D2-10 — the residue sweep

`retire-with-a-sweep`: the unfair semaphore and any `Semaphore(1)` comment describing it; the
discarded-seqNo statements; `CURSOR_INVALID`'s message; the `EngineTestHarness` name; the
`engine-ports.v1.json` `health` row; `verified-facts.md`'s D2 citations (§0); the "three SQLite
stores" sentence in 17.3 row D2 and `verified-facts.md` (four). Acceptance: `WholeProgramDeadCodeTest`
with a shrinking baseline; the full suite.

---

## 4. Commit plan

One commit per item, `feat(936): D2-n — …`, green after every item. D2-4 carries a table of the
clauses of 4 and the test that pins each.

## 5. Section 6 contract changes and their gates

| change | gate |
|---|---|
| `GET /api/engine/components` (new); `engine-ports` `health` row corrected | `--gate engine-port`; `regen-all --check`; contract-surface register |
| `CURSOR_EXPIRED` (new response class reaching the webview) | `ErrorMessagePropertiesContractTest`, `ErrorCatalogJsonArtifactTest`; `run-ui-web-gates` |
| `SearchTrace` gains two stage timings | `--gate execution-surface` |
| port method `indexAndReturn` (port-level only, no HTTP/MCP) | `--gate engine-port` (consumers list) |
| dev-runner `--profile` | `test-dev-runner-*.mjs` via B9's runner |

## 6. Section 16 rows D2 must leave exercisable

request-time encoders (the unit clauses); durability; aggregate bound (the cursor clause);
generation transition (the cursor clause); recovery workflow (the generic MCP client); positive
benefit (verification-profile boot time); combined delayed retry (the component-map clause).

## 7. Harness

D2-4's gate tests are deterministic unit tests; D2-5, D2-6 and D2-9 run in the installed-process
tier beside B17's and C2's scenarios; D2-8 extends `encoder-latency-probe.sh`. No new driver.

## 8. Governance changes

| register / check | what D2 does |
|---|---|
| `governance/engine-ports.v1.json` | the `health` row names the component map; `indexing` port consumers gain `indexAndReturn` |
| `governance/retained-state.v1.json` (C1) | `cursors` and `pinned readers` get producers; `awaitingProducer` cleared |
| `governance/execution-surfaces.v1.json` | the two trace stage timings' referencers |
| `errors.en.properties` / `SSOT/messages/errors.en.json` | `CURSOR_EXPIRED` added; `CURSOR_INVALID` reworded |
| `scripts/ci/stress-suite-policy.v1.json` | any `@Tag("stress")` test D2 adds (B17's rule) |

## 9. What is allowed to be red after D2, and nothing else

1. The verification-profile boot-time **number** is E's to record; D2's test pins the first-cut
   bound only.
2. The session gate's aging threshold and batch bound are first cuts; E confirms them.

Not allowed red: the full suite; both supervisor adapters; `engine-port`, `execution-surface`;
`run-ui-web-gates`; search, ingestion and the dev stack; the installed-process tier.

## 10. Stop rule (17.8)

If pinning a reader per cursor cannot be made to release Windows handles on generation
retirement (the 915 B5 class again), stop and re-cut D2-6 to "cursors are generation-bound and
fail `CURSOR_EXPIRED` at activation, never pinned across it", recording why; do not weaken the
handle test. If the gate cannot be made fair without a measurable foreground-latency cost above
E's row, stop and record the number before choosing.

## 11. Decisions recorded for D2 (design section 0, 2026-09-09)

- The component map is one new endpoint over D1-1's registry plus C1's two registers; the
  three existing runtime surfaces and the build-time exporter become projections of its
  snapshot; the `engine-ports` `health` row is corrected to name it.
- `verification` boots with `encoders` and `generative` `absent`; its boot-time bound is a first
  cut of five seconds, E records the number.
- `CURSOR_EXPIRED` is a new code; `CURSOR_INVALID` keeps its meaning (malformed) and gets an
  honest message.
- Index-and-return plumbs Lucene's sequence numbers to the coordinator so `durable` can wait on
  a covering commit; it stays port-level in lane F.
- The generic MCP-client recovery harness is D2's (D2-9), not C2's.
- Four SQLite stores, not three, get the ephemeral variant.

## 12. Implementation batches

| batch | items | verification | may be red after |
|---|---|---|---|
| **1 — profiles, library, ephemeral stores, component map** | D2-2, D2-3, D2-7, D2-1 | `:modules:app-engine:test` `:modules:ui:test` `:modules:indexer-worker:test` `:modules:worker-core:test`; `--gate engine-port`; `regen-all --check`; `run-ui-web-gates`; the dev-runner test runner | nothing |
| **2 — the session gate and the trace timings** | D2-4, D2-8 | `:modules:ort-common:test` `:modules:worker-services:test` `:modules:app-inference:test`; `--gate execution-surface`; the **full suite** | §9 row 2 |
| **3 — index-and-return, cursors, the MCP harness** | D2-5, D2-6, D2-9 | `:modules:adapters-lucene:test` `:modules:core:test`; `check-readiness-reason-codes`; `run-ui-web-gates`; `:modules:system-tests:integrationTest` for the three new scenarios; the **full suite** with `-PincludeStress=true` | nothing |
| **4 — sweep** | D2-10 | `WholeProgramDeadCodeTest`; full suite | §9 only |

---

*End of the stage-D2 checklist. Drafted 2026-09-09 at `be47faa40` during stage C planning;
re-ground every citation at stage start. Corrections found while implementing go to §0.1.*
