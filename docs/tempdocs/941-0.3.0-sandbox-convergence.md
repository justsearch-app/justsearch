---
title: "JustSearch 0.3.0 Sandbox convergence — qualifying rounds for the 0.3.0 candidate"
type: tempdocs
status: "closed 2026-09-07 — 0.3.0 published from main@59ae966dd (round 20 skipped, gestalt pass waived by the owner); WinGet distribution retired end to end the same day"
created: 2026-09-06
updated: 2026-09-07
related:
  - 938-release-consolidation-before-architecture-change
  - 939-codex-sandbox-verifier
  - 734-0.2.0-sandbox-convergence
  - 823a-review-addenda
---

# JustSearch 0.3.0 Sandbox convergence

This is the 0.3.0 counterpart of tempdoc 734: one section per Sandbox round, written
host-side from `tmp/sandbox/share/evidence/` after the round finalizes, plus the routing
of every confirmed finding to a regression home. The durable harness method lives in
`scripts/sandbox/*.md`; the release runbook is `docs/how-to/cut-a-release.md`.

## Candidate lineage

| Candidate | Built from | CI run | Signed | Notes |
|---|---|---|---|---|
| `JustSearch_0.3.0_x64-setup.exe` (round 18) | `main@ef2252714` (release(938): 0.3.0 consolidation, #695) | 34052189315 | no | sha256 `fbf6c679629ec4b423a66fd65723dd2f281cca3f42b429753420a719adb5a5fc`; first 0.3.0 candidate; Search v3 not promoted |
| `JustSearch_0.3.0_x64-setup.exe` (round 19) | `main@6bff10e4a` (fix(941): round-18 fixes, #702) | 34082273197 | no | sha256 `38504446257a307c8253470bea075fe12ddd9a315b6cbe0b6551ec79ce8f36e0`; candidate 2, carries the round-18 fix campaign; golden baseline reused from candidate 1 |
| (round 20 — not built) | `main@59ae966dd` (fix(941): round-19 fixes, #703) | 34093637623 (cancelled) | — | unsigned candidate-3 dispatch cancelled after the owner skipped round 20; the release build is the signed `v0.3.0` tag dispatch of the same commit |

## Qualifying set for 0.3.0 (runbook step 2)

- [x] at least one `upgrade-from-release` round over the exact published v0.2.0 (823a §1
      item 2) — **round 18** (all five `upgrade-*` ids observed-pass)
- [ ] first and final qualifying rounds `fresh-install` (`--no-models`)
- [~] owner gestalt manual pass without API access — **waived by the owner 2026-09-07**
      ("this time a gestalt pass isn't necessary"); not a qualifying condition for 0.3.0
- [x] host-side finalize green: `check_coverage.py`, `check_golden_parity.py`, six process
      artifacts, `mutating-probe.v1.json` pass — round 18 (re-required for every later round)
- [ ] every blocking finding fixed and re-confirmed, or explicitly downgraded to a Known Issue

## Round 17 — 0.2.0 final qualifying round (pointer)

Recorded in full in tempdoc 734. Fresh-install round on the signed 0.2.0 rebuild of
2026-08-13; the candidate qualified and was published as v0.2.0 the same day. The 823a §1
upgrade lane (over-install from a same-publisher-key install) was pre-registered there for a
future upgrade round and is what round 18 below runs.

## Round 18 — first 0.3.0 round: upgrade-from-release over published v0.2.0 (staged 2026-09-06)

- Charter: [`941-evidence/round-1-charter.md`](941-evidence/round-1-charter.md) (round 18) — the
  823a upgrade lane, the five registered `upgrade-*` must-watch ids, and the PR #695 fixes
  (868 / 877 / 909 / 911 / 875 undo gate) with healthy-vs-broken signatures per item.
- Previous release: the GitHub Release asset `JustSearch_0.2.0_x64-setup.exe`
  (published 2026-08-13, signed, CN=Elias Justus), downloaded to `tmp/release-v0.2.0/`
  and verified against the release's `SHA256SUMS` before staging.
- Mode: `upgrade-from-release` (implies no host models mapped).
- Golden-parity baseline: generated 2026-09-06 with `gen_golden_parity.py` against a dev
  stack built from the candidate content (worktree merge of #695 onto `7b34390bd`, identical
  tree to `ef2252714`), SciFact 5,184 files (5,189 indexed docs), embed and SPLADE at 100%,
  cuda EP, all six retrieval legs executed; `gitHead` in the file set to `ef2252714` by hand
  because the generator records the host checkout's HEAD, which was one commit behind.
- Verifier: independent agent inside the Sandbox; Codex first (tempdoc 939's first graded
  Codex round — its five open items are answered in this round's `session-analysis.md`),
  Claude Code fallback.
- Verifier as run: **Codex** (gpt-5.6-sol, `danger-full-access`, Computer Use via
  `@oai/sky` through `node_repl`) — the first graded Codex round. Elapsed ~3.8 h
  self-reported (setup 0.35, install-wait 0.80, coverage 1.55, findings 0.70, write-up 0.40).
- Result (host-side finalize, 2026-09-07): **upgrade lane PASSED; all host-side checks
  green after one in-session correction** — four product findings (one MEDIUM, three LOW) and
  two harness findings, none blocking; one finding withdrawn on retry.

| Host-side check | Result |
|---|---|
| `check_coverage.py` | exit 0 — 26/26 mustTouch covered. First run was exit 1 with **`shape:core.rag-ask` uncovered** (token `rag-ask` in no filename): the Documents-rung answer WAS captured (`56-candidate-unified-chat-ask-answer.png`: Document Q&A frame, "Based on your documents", one source card) but named after the harness doc's own example `13-unified-chat-ask-answer.png`, which lacks the shape token. The verifier renamed it in-session to `56-candidate-unified-chat-rag-ask-answer.png` (rename, not copy; both tokens in one name) and re-listed it; the host never touched the evidence. Harness doc fixed in this branch (`scripts/sandbox/sandbox-CLAUDE.md`). Six process artifacts PRESENT; evidence review 75/75 examined, 0 mismatches; duplicate-content OK; mutating probe pass. |
| `check_golden_parity.py` | exit 0 — 10/10 blocking (golden #1 in top-3); descriptive: q04 6/10, q06 5/10, q08 4/10 (the known dev→Sandbox systematic divergence, 734 finding 5); all 10 flagged `PARITY_EMBEDDING_VARIANCE` (max dense delta 3.6e-2). |
| `check_token_health.py` | exit 0 — 10,926 HTTP spans, no sub-5 ms 401 on a mutating route. |
| must-watch verdicts | 13/13 recorded: 12 observed-pass, `install-trust-prompts` unobservable (unsigned candidate, as chartered). |

**Upgrade lane (823a §1 item 2) — all five `upgrade-*` ids observed-pass**: docCount 5,190
preserved across over-install, `embeddingFingerprintCurrent` byte-identical / COMPATIBLE,
`chunkEmbeddingReady` true through over-install, genuine four-process cold restart and warm
reinstall; exactly one HKCU uninstall entry, zero 0.2.0 jars beside 0.3.0 jars, mid-upgrade
uninstaller left "Delete the application data" unchecked; passphrase, memory and skin survived.
877/868/909 signatures observed healthy (typed `errorCode`/`retryable` on the tool card, withheld
tools named, the across-restart turn found in `agent-history` with no `.md.pending` marker).
Undo trust gate (875) and MCP TYPED_CONFIRM ceremony observed. Known Issues recorded as
observed-known, not re-filed.

### Findings

Host verification column = what I re-checked against the evidence and `main@ef2252714` before
routing; the round's own text is in `tmp/sandbox/share/evidence/findings.md` (archived with the
round).

| # | Sev (round) | Claim | Host verification | Status |
|---|---|---|---|---|
| F1 | MEDIUM | Structured rung clears the prompt without dispatching; no `POST /api/chat/extract` span; direct extract control 200 | **Mechanism corrected.** The shipped UI never calls `/api/chat/extract` — `UnifiedChatView.send()` posts every shape to `/api/chat/dispatch` with `shapeId` (`core.extract` when a schema is attached). The trace has dispatch spans at both Structured-send times (20:47:44 UTC 1,080 ms and 20:49:08 UTC 710 ms, both 200) versus 17 s for the direct extract control. So the webview DID dispatch and the backend answered in ~1 s with something the UI rendered as no new JSON (`62-…-extraction-result.png` shows the prior agent answer under the extract frame label). **Post-finalize retry (candidate reinstalled over retained data): the same schema and prompt through the real UI rendered a new JSON extraction (`89-candidate-unified-chat-extract-result.png`: `{"title":"Essential Troubleshooting Guide…"}`, 1.1 s, dispatch count 13→15).** The verifier withdrew F1. The two original 1-s dispatches with no rendered JSON remain unexplained (the retry ran on a fresh shell instance, not the long-running upgraded one that also showed F3) — carried as a watch item, not a finding. The direct control's `question` field was the wrong key (`/api/chat/dispatch` wants `prompt`, `MISSING_PROMPT`). | withdrawn on retry; watch item |
| F2 | MEDIUM | failed-files API row and drawer carry no scan id (911 fix missed) | **API half refuted, drawer half stands as a gap, charter at fault.** The round captured `GET /api/indexing/failed-jobs` — the legacy handler whose `FailedJob` record has never carried `scanId` by design. The 911 plumb is on `/api/indexing-jobs/failed` and `/by-prefix` (`IndexingController.toJobView`, wire-contract test green), which the drawer does fetch (one by-prefix span in the trace) but **`FailedJobsDrawer.ts` does not render `scanId`** — 911 was a wire-level fix, and my charter item 7 overstated it as a visible drawer id. **Post-finalize: `GET /api/indexing-jobs/failed` on the reinstalled candidate returned the row with `scanId` `959dd932-…` (`failed-jobs-substrate-api.json`) — the 911 plumb works end to end; the id differs from the first scan's `b974d057-…` because the reinstall re-scanned and the file failed again (`lastUpdatedMs` advanced), which is the queue's own value as designed.** | downgraded to LOW (drawer display gap); wire plumb verified live; charter imprecision noted |
| F3 | MEDIUM | Settings shows raw `settings.*` i18n keys in the long-running upgraded shell; cold restart fixes it | Confirmed from `78-candidate-settings.png` (search placeholder, groups, categories, sections all raw keys, version 0.3.0) vs the settled control after restart. Lifecycle race in the shell's message catalog, not a missing catalog. | confirmed |
| F4 | LOW | Health event text `An indexing job failed for {path}: {errorClass}.` plus raw `atMs=…` attributes | Consistent with `healthEventActivityRow.ts` presenting an un-substituted template; screenshot 74. Not re-derived beyond the capture. | accepted |
| F5 | LOW | Add Folder during schema rebuild shows "Recoverable condition" with `rebuild-index` and a raw GMT timestamp | Accepted on the round's raw-frame capture; the same flow succeeded after the rebuild. | accepted |
| H1 | LOW | Codex Step-0 probe reports `apps=[]` on the unified `cua_repl`; `@oai/sky` via `node_repl` works | Harness (tempdoc 939 open item 1 answered: Computer Use works, through the skill's `node_repl` path only). | harness |
| H2 | MEDIUM | `gui/snap.ps1` `CopyFromScreen` → "The handle is invalid" on the Sandbox's RDP indirect display; per-window capture and Windows Graphics Capture work | Harness: the documented "guaranteed floor" is not one on this display stack. | harness |

Tempdoc 939 open items, answered by `session-analysis.md`: (1) Computer Use saved screenshots
to the coverage filenames directly but as **JPEG bytes under `.png` names** — the round's
`round-tools/convert-cu-images.ps1` repaired them; (2) the `JustSearch.exe` allowlist
suppressed every approval prompt; (3) no rate-limit stall; (4) `AGENTS.md` was read in full
(the Independence-invariant section was visible, so `project_doc_max_bytes` took effect).

Charter imprecision recorded (harness finding, mine): item 7 named the legacy
`/api/indexing/...failed-jobs` path and promised a visible drawer scan id; the correct
discriminator is `GET /api/indexing-jobs/failed` (row `scanId` == the `POST /api/knowledge/ingest`
`scanId`). Item 7's zero-byte-PDF / locked-file triggers also produced no failed row — the
round needed a corrupt `%PDF` body.

### Routing

- **F1 (Structured dispatch)** → withdrawn; watch item for the next round's charter: on the
  long-running (not freshly restarted) shell, take a dispatch-span count before and after one
  Structured send and confirm a rendered JSON result tied to the prompt — the two-minute binary
  check the round's own session-analysis proposed.
- **F3 (raw i18n keys)** → fix campaign; regression home = a ui-web unit/RAIL assertion that no
  rendered Settings label starts with `settings.` after a backend restart under an open shell.
- **F2 (drawer scan id)** → owner decision: render `scanId` in `FailedJobsDrawer` (LOW) or
  declare the wire plumb sufficient; either way the charter template for the next round names
  `/api/indexing-jobs/failed`.
- **F4, F5 (copy)** → fix campaign as LOW; regression home = ui-shot assertions on the seeded
  failure event and the rebuild-time Add Folder notice.
- **H1, H2** → `scripts/sandbox/`: Step-0 probe tries `@oai/sky` before declaring API-only;
  `snap.ps1` falls back to Windows Graphics Capture on `E_HANDLE`; Codex screenshot export
  verifies magic bytes. Plus the `rag-ask` filename guidance (fixed here) and the failed-file
  fixture (corrupt `%PDF`, not zero-byte).
- **Coverage gap `core.rag-ask`** → closed in-session (verifier renamed, host re-ran all three
  checks: coverage 26/26, parity 10/10, token health clean — independence invariant intact,
  the host never edited evidence).

## Round-18 fix campaign (landed 2026-09-07)

Three parallel workers on isolation worktrees, integrated on `worktree-942-fix-campaign`; every
fix carries a test that failed on the pre-fix code and passes after (fail-before output was
required in each brief and quoted in each report).

| Finding | Root cause (primary source) | Fix | Regression test |
|---|---|---|---|
| **F3** raw `settings.*` keys (MEDIUM) | `bootMessageCatalog` in `modules/ui-web/src/i18n/resourceCatalog.ts` was one terminal `fetch` (`if (!response.ok) return;` + swallowing `catch`) with no retry and, unlike `bootResourceCatalog`, no persisted body. It is fired once at module evaluation (`src/i18n.ts`), and the shell reloads itself the moment the Rust side sees a new Head instance (`main.jsx` → `installBackendRestartBridge` → `window.location.reload()`), so the boot fetch races a Head that publishes a manifest before it answers `/api/messages/**`. One lost race = raw keys for that document's life. Second, independent path: `MessageCatalogController` serves `Cache-Control: public, max-age=3600` + strong ETag, and the fetch was cache-permissive, so a WebView2 cache surviving an over-install could answer with the previous version's body (which knows no `settings.*` key — all 35 are new in 0.3.0, 855 `dbe3314d5`). | Bounded retry (250 ms → 20 s, six attempts, ~32 s budget) and `cache: 'no-cache'` (end-to-end revalidation against the ETag). No fallback strings: an exhausted budget still renders raw, by design. | `SettingsNav.catalogBoot.test.ts` (new): the boot fetch races a restarting backend (reject → 503 → 200) and a pre-upgrade HTTP-cache body; both assert no rendered label starts with `settings.`. Pre-fix: 19 raw keys, exactly the field set. |
| **F4** `{path}` / `{errorClass}` template + `atMs=…` attribute dump (LOW) | Two faults. The catalog (`health-events.en.properties`) asked for `{errorClass}`, but the only emitter (`WorkerSnapshotTap.detectJobFailureOccurrence`) sends `path`, `errorMessage`, `atMs`. And the frontend never interpolated parameters at all — `bodyToMessage` in `healthEventActivityRow.ts` flattened the attributes map to `k=v` as a last resort. | Catalog asks for `{path}` / `{errorMessage}`; new `interpolateMessage` in `resourceCatalog.ts` (declines, never leaks a brace; `Object.hasOwn`); the activity row passes the attributes as parameters and drops the dump. | `healthEventActivityRow.test.ts` (+3): a contract test that reads the catalog AND the Java emitter and asserts every placeholder names an emitted attribute; a substituted-sentence test; a missing-parameter fallback test. |
| **F5** `rebuild-index` + GMT timestamp headline on Add Folder during a rebuild (LOW) | `AdvisoryToastHost.ts` was the last advisory surface rendering raw ids: headline was the class-generic label, the "action" label was `target.split('.').pop()` of `core.rebuild-index`, the time was `toLocaleTimeString()`, and the authored `bodyI18nKey` was never rendered. The condition is `index.unavailable` (recovery `core.rebuild-index`). | Headline and button through `present()`, time through `formatRelativeIso`, authored body rendered (and interpolated); the ids stay behind the app-wide Detailed disclosure. | `AdvisoryToastHost.test.ts` (+5), including a clock-shape assertion (happy-dom emits no `GMT` token, so "no GMT" alone would have passed pre-fix). |
| **F2** drawer omits the substrate scan id (LOW) | `FailedJobsDrawer.ts` parsed `scanId` (on the by-prefix contract since 911) but `renderRow` never read it. | Renders `Scan <id>` (full id in `title`) only when non-empty; `""` is the contract's "no owning scan". | `FailedJobsDrawer.test.ts`: renders per row, omits the line for a row without a scan. |
| **H1** Codex probe declared API-only on an empty inventory | `sandbox-start-SKILL.md` Step 0 read the unified inventory only. | Step 0 probes `@oai/sky` through `node_repl` before reading the inventory; PNG magic bytes verified on the first capture. | Host-run: `check-sandbox-authorization-field` OK, `sandbox-launch.py --help` OK. |
| **H2** `snap.ps1` died with `E_HANDLE` on a dead desktop DC | Single capture path in `JustSearchGui.psm1`. | `Save-AppShotPrintWindow` (PrintWindow + `PW_RENDERFULLCONTENT`) fallback, `-ForceWindowCapture` / `-Hwnd` / `-ProcessName`; `convert-cu-images.ps1` promoted into `scripts/sandbox/gui/` with magic-byte classification. | Host probe: `snap.ps1 -ForceWindowCapture -ProcessName notepad` → 15,309-byte PNG; `check-ps1-warning-comments` OK, `npm run lint:scripts` clean. |

Verification on the integrated branch: `npm run typecheck` clean; `npm run test:unit:run`
473 files / 6351 tests passed; `run-ui-web-gates.mjs` 27/27; `./gradlew.bat build -x test`
green (one first pass reddened `LambdaMartBenchmarkTest` p50 49 ms while vitest ran alongside
it — a load artefact, re-run alone: green); worker-side `:app-services:test`,
`:app-api:test`, `:ui:test` green (2574 / 195 / 993, 0 failures).

Seen, not fixed (routed here as the owning tempdoc's open items — none blocks 0.3.0):

- `present.ts` condition-case i18n branch is dead (looks up the bare condition id, every key is
  `health-events.<id>.label`), so all three consumers take the humanized fallback ("Index
  Unavailable" not "Indexer unavailable"); `health-events.<id>.reason.<Code>.message` keys have no
  consumer; `health-events.schema.rebuilding.*` is a phantom id (no emitter).
- The same one-shot + cache-permissive fetch pattern as F3 remains in `errorCatalog.ts` and the
  five registry clients (they at least persist their body); there is still no in-place
  "re-boot the catalogs" entry point — recovery depends on a reload winning the race.
- `resolveBootApiBase` has no readiness wait, so the whole boot chain races a starting Head.

## Round 19 — first fresh-install round on candidate 2 (pre-registered 2026-09-07)

Charter: `941-evidence/round-2-charter.md`. Mode `fresh-install`, `--no-models`, Codex
verifier first. Candidate 2 = unsigned `build-installer.yml` dispatch on `main@6bff10e4a` (run 34082273197,
verified against its `SHA256SUMS`; lineage row above). Items 1–6 of the charter
re-confirm F2/F3/F4/F5 and the Structured watch item with their healthy signatures; the five
`upgrade-*` ids are unobservable in this mode. Golden baseline: the candidate-1
`golden-parity.json` is reused (same corpus and weights; no search-affecting change in the
campaign — `check_golden_parity.py` does not bind the baseline to a commit).

- Verifier as run: **Codex** (`@oai/sky` Computer Use via `node_repl` after the unified inventory
  returned `apps=[]` — the amended Step-0 probe was load-bearing; the staged converter repaired
  JPEG-under-`.png` captures as predicted). Elapsed ~1.1 h self-reported (setup 0.12,
  install-wait 0.18, coverage 0.47, findings 0.18, write-up 0.17).
- Result (host-side finalize, 2026-09-07): **all host-side checks green; candidate 2 does NOT
  qualify** — one HIGH, two MEDIUM, one LOW product finding, one harness finding. Round-18 fix
  re-confirmation: F3 (Settings i18n), F4 (health-event copy) and F5 (rebuild notice) PASSED;
  F2 (drawer scan id) did not — the by-prefix row itself carried `scanId:""` (backend, see
  R19-F2). The round-18 Structured watch item reproduced (R19-F1).

| Host-side check | Result |
|---|---|
| `check_coverage.py` | exit 0 — 26/26 mustTouch covered; six process artifacts PRESENT; evidence review 49/49 credit-eligible screenshots examined, 0 mismatches (5 bulk frames under `raw-frames/` excluded by convention); mutating probe pass. |
| `check_golden_parity.py` | exit 0 — 10/10 blocking (golden #1 in top-3); descriptive sub-floor overlap again on q04 (6/10) and neighbours — the known dev→Sandbox divergence (734 finding 5). Baseline reused from candidate 1 (fingerprint `f1d0f4ec…`, 5,189 docs). |
| `check_token_health.py` | exit 0 — 7,783 HTTP spans, no sub-5 ms 401 on a mutating route. |
| must-watch verdicts | 8/8 mode-included ids recorded: 5 observed-pass, `install-trust-prompts` and `round-10-f11-mid-upgrade-uninstaller-window` unobservable (unsigned build / fresh-install mode, as chartered), **`ui-api-truthfulness-under-load` observed-fail** (R19-F4 below). |

Fresh-install journey: installer → first paint → Install AI through the API start with every UI
state observed (10.14 GB real download, GPU_FULL) → SciFact + small folder → WebView search →
cited Documents answer → Structured → Agent (hit the 10-iteration ceiling on a read-oriented
task, known) → authenticated `/mcp` + MCPB TYPED_CONFIRM → Worker kill and recovery → genuine
four-process cold restart → preserve-data uninstall/reinstall → uninstall. Coverage limit: the
privacy recovery-key screen was not committed (irreversible passphrase boundary under a
no-input run) — inspected, not executed.

### Findings

| # | Sev | Claim | Host verification (evidence + `main@6bff10e4a`) | Status |
|---|---|---|---|---|
| R19-F1 | HIGH | Structured (one-property schema, bottom mode control) in the long-running shell: dispatch count 3→4, HTTP 200 in 1,039 ms, prompt cleared, no "Model-generated structure" frame, no JSON. Fresh-shell control (new conversation via "Start fresh" + "+ Schema") rendered `{"phrase":"quick brown fox"}`. | Follow-up capture after a preserve-data reinstall (`evidence/post-round/round19-f1-followup/`): `/history` and `/api/thread/<id>` both hold the extract user + assistant records (`shapeId: core.extract`, content `{"phrase":"quick brown fox"}`) in the SAME conversation as the earlier rag-ask turn. The shell fetched that history right after the 200 and kept showing the previous Document Q&A card. Backend generated and persisted; the shell failed to reconcile. The 250 ms dispatch at 05:25:05 was automatic title generation, not a user turn. | confirmed — fix campaign |
| R19-F2 | LOW | Failed-files drawer shows no scan id although the global `GET /api/indexing-jobs/failed` row carries one. | `44-failed-jobs-by-prefix.json` vs `39-failed-jobs-drained.json`: same `pathHash`, later `lastUpdatedMs`, `scanId:""` and `collection:""` on the by-prefix row. Both endpoints map through one `toJobView` and one SQL select; the ROW changed between reads — the periodic `syncDirectory` walk re-enqueues every un-indexed file through the untagged `enqueueEntries(batch)` and the `INSERT OR REPLACE` reset both columns to NULL. Second defect: `toJobView` defaulted `collection` on `null` only, but proto3 hands `""`. The #702 drawer change was correct and starved. | confirmed — fix campaign |
| R19-F3 | MEDIUM | Clean Install AI ends `installedFully:false`, message "Installed with limitations: Chat model (compact) skipped on this hardware." while the UI says "Install AI completed." | `51-ai-install-status-final.json`: all user packages `installed`, `chat-compact` skipped with a development-only `skipReason`; the completion message attributes the skip to hardware. A clean install can never satisfy its own completion contract. | confirmed — fix campaign |
| R19-F4 | MEDIUM | After a genuine cold restart, the reopened rag-ask answer keeps its text and five sources but the card reads "Model answer — this mode does not search your documents". | `57-…rag-ask-answer.png` vs `77-…after-cold-restart.png`, `74-cold-restart-proof.json`. `answerFrameLabel` yields that string only for `core.free-chat`; on reload `UnifiedChatView` framed records with `currentShapeId()` (the window's current affordance, `'none'` after restart) instead of the record's own persisted `shapeId`, which the backend has carried since 863. | confirmed — fix campaign |
| R19-H1 | LOW (harness) | `collect-evidence.ps1` printed app-data creation 04:55:19Z > boot 04:32:59Z yet "predates boot: True"; the exe creation time (04:23:40Z) genuinely predates boot. | `Win32_OperatingSystem.LastBootUpTime` is a Local-kind DateTime compared by raw ticks against `CreationTimeUtc` — reproduced on the host (UTC+1: a file created 20 min after boot reads "predates"). The exe timestamp is installer-preserved from the CI build, so it is not a freshness oracle. | confirmed — fixed in this campaign |

Not re-filed (carried over, observed-known): reasoning-only completions consuming the whole
allowance (845/848), the 10-iteration agent ceiling (868), SmartScreen/elevation unobservable,
golden sub-floor overlaps, the coverage brief's generic signed-candidate line conflicting with
the per-round unsigned charter (charter wins; harness imprecision).

### Routing

- **R19-F1** → fix campaign. Routed as UI reconciliation; the campaign found the persisted
  records render on the pre-fix view and the actual mechanism is that the transcript never
  followed its tail (see the round-19 fix campaign below). Must-watch id
  `structured-long-running-dispatch-render` registered with the fresh-shell control.
- **R19-F2** → fix campaign (`SqliteJobQueue` carry-forward of `collection`/`scan_id` on an
  untagged re-enqueue; `toJobView` blank-collection default). Open item: `SyncDirectoryOps`
  enqueues NEW files with no collection at all (needs `collection` on `SyncDirectoryRequest`).
- **R19-F3** → fix campaign (development-only packages out of the `installedFully` denominator
  and out of the hardware-skip message).
- **R19-F4** → fix campaign (`recordShapeId(it)` reads the persisted `shapeId`, falls back to
  the window mode only for pre-863 rows). Open item: `resumeConversation` in
  `conversationListStore.ts` still drops the per-message `shapeId`; `rag.standaloneQuestion`
  is not persisted on the assistant record.
- **R19-H1** → `scripts/sandbox/collect-evidence.ps1` (boot time normalized to UTC; the install
  DIRECTORY's creation time replaces the exe's as the freshness signal).
- Harness/charter: the coverage brief's signed-candidate validation line should defer to the
  per-round charter's signing statement; the round's own proposal of a non-destructive
  recovery-key preview fixture is recorded here, not scheduled.

## Round-19 fix campaign (landed 2026-09-07)

Four parallel workers on isolation worktrees plus one host-side harness fix, integrated on
`worktree-942-fix-campaign`; every product fix carries a test that failed on the pre-fix code
and passes after (fail-before output quoted in each report).

| Finding | Root cause (primary source) | Fix | Regression test |
|---|---|---|---|
| **R19-F1** Structured result "lost" in the long-running shell (HIGH) | Not reconciliation — the follow-up capture's verbatim records render on the pre-fix view too. `#run-conversation` (`UnifiedChatView.ts` ~2716) is the surface's scroll region and nothing ever scrolled it: zero `scrollTop` / `scrollIntoView` / `scrollHeight` references in the view; the only `scrollIntoView` in the chat chain is the user-driven J/K spine jump in `primitives/navigation.ts`. A new turn on a conversation taller than the column renders below the fold; the reader sees the previous card, unchanged. The fresh-conversation control passed for the one reason a fresh shell differs: an empty conversation has no scroll offset. Second, independent silent-loss branch: `onDone` committed the turn only `if (this.streamingText.trim())` and ignored the `done` payload's `finalResponse` (`ConversationEngine.emitDone`, line 1216) — a run whose `chunk` frames never accumulated in the view (a per-event handler throw is swallowed to `console.warn` in `api/streams.ts`) dropped the answer with no message after the composer had already cleared. | Follow-tail: `followTail` / `pinConversationTail()` from `updated()`, `@scroll` on the conversation div releases it when the reader scrolls up (24 px slack), re-armed on the reader's own submit. `onDone` prefers streamed text, falls back to `finalResponse`, and renders an explicit "returned no result" notice when both are empty (guarded on an already-reported `errorMessage`). | `UnifiedChatView.extractResult.test.ts` (new, 8 cases): A–C/F drive the captured `thread-live-after-reinstall.json` records through the view; D (`finalResponse` only), E (no-result notice) and G (long conversation follows its tail, `scrollTop` 0 → 1500 pre/post) failed pre-fix. Falsifier for round 20: if the failure recurs, scroll the transcript down — the card sitting below the fold confirms the mechanism. |
| **R19-F2** by-prefix row loses `scanId` / `collection` (LOW) | The periodic `SyncDirectoryOps` walk re-enqueues every un-indexed file (including permanently FAILED ones) through the untagged `enqueueEntries(batch)`; `SqliteJobQueue`'s `INSERT OR REPLACE` upsert then reset both columns to NULL. Second defect: `IndexingController.toJobView` defaulted `collection` on `null` only, while proto3 hands `""`. | `SqliteJobQueue` upsert carries `collection` and `scan_id` forward with `COALESCE` from the prior row when the new entry is untagged (javadoc on `JobQueue`); `toJobView` maps blank → `default`. | `JobQueueTest.maintenanceReenqueuePreservesCollectionAndScanId`; a new `IndexingControllerFailedJobsWireContractTest` row for the blank collection. |
| **R19-F3** clean Install AI ends `installedFully:false` (MEDIUM) | `InstallPlanner` emits `SkipCause.DEV_ONLY` for `chat-compact` (`model-registry.v2.json` `devOnly:true`), and `AiInstallService.applyCompletionState` computed `installedFully = !requiredMissing && skippedCount == 0` — every skip counted, and the completion message attributed every skip to hardware. | `SkipCause` gains `id()` / `limitsInstall()` / `fromId`; `populateStatusPackages` drops DEV_ONLY rows and stamps `skipCause` on the remaining skipped rows; `installedFully` keys on limiting (HARDWARE) skips, unknown ids fail closed to HARDWARE. New wire field `PackageStatus.skipCause` (schema and generated TS regenerated). | `AiInstallServiceDevOnlyCompletionTest` (5 tests): dev-only skip → `installedFully:true` and no chat-compact row; hardware skip still limits; unknown cause fails closed. |
| **R19-F4** reopened rag-ask turn loses "Based on your documents" (MEDIUM) | On reload `UnifiedChatView` framed every record with `currentShapeId()` — the window's current affordance, `'none'` after a restart — not the record's own persisted `shapeId` (carried since 863). | `recordShapeId(it)` reads the persisted `attributes.shapeId`, validated against `CORE_INTERACTION_SHAPES`, falling back to `currentShapeId()` only for pre-863 rows. | `UnifiedChatView.test.ts` (+3): persisted rag-ask record keeps its frame after resume with the affordance at `none`; pre-863 fallback; invalid id falls back. |
| **R19-H1** `ARTIFACT-PREDATES-BOOT` false positive | `Win32_OperatingSystem.LastBootUpTime` is a Local-kind DateTime compared by raw ticks against `CreationTimeUtc`; the exe timestamp is installer-preserved from CI and not a freshness oracle. | `collect-evidence.ps1` normalizes boot time with `.ToUniversalTime()` and fingerprints the install DIRECTORY's creation time, printing the exe time as information only. | Host proof: `kind=Local tz=01:00:00; raw compare (bug): True; normalized compare (fix): False`; `check-ps1-warning-comments` OK. |

Must-watch: `structured-long-running-dispatch-render` registered in
`governance/sandbox-coverage.v1.json` with the fresh-conversation control and the
history-capture discriminator in its `validateHow`.

Verification on the integrated branch (run sequentially, never Gradle beside vitest):
`npm run typecheck` clean; `npm run test:unit:run` 474 files / 6362 tests passed;
`run-ui-web-gates.mjs` 27/27; `regen-all --check` 8/8 generated sets match;
`npm run lint:scripts` clean; `check-ps1-warning-comments`, `check-tempdoc-numbers`,
`check-tempdoc-size`, `check-sandbox-authorization-field` OK; `./gradlew.bat build -x test`
and the affected module tests (`indexer-worker`, `worker-core`, `ui`, `app-services`,
`app-api`, `configuration`) — see the review record for the counts.

Seen, not fixed (routed here as the owning tempdoc's open items — none blocks 0.3.0):

- `SyncDirectoryOps` enqueues NEW files discovered by the periodic sync with no collection at
  all (`SyncDirectoryRequest` carries none), so a file first seen by the sync under a
  collection-tagged root lands in `default`; the F2 carry-forward only protects rows that were
  tagged once.
- `resumeConversation` in `conversationListStore.ts` still drops the per-message `shapeId`;
  `rag.standaloneQuestion` is not persisted on the assistant record.
- The frontend does not show the per-package `skipReason` / `skipCause` (the new wire field has
  no consumer yet); the message catalog still carries only the hardware wording.
- `UnifiedChatView.renderLiveOverlay` dedups on a plain `Set` of kind+content while
  `mergedTimeline` is occurrence-aware, so a repeated identical turn can vanish from the live
  overlay; `api/streams.ts` swallows a throwing per-event handler to `console.warn`, which is
  what makes the `finalResponse`-only path reachable at all.
- `PreOpenSchemaMismatchBootTest` (`indexer-worker`) can fail in `@TempDir` teardown on Windows
  under parallel module-test load: `entity-clusters.db-wal` is still held when JUnit deletes the
  directory (test body green; class alone green). The boot fixture should close the entity-cluster
  store before the directory is torn down, or the class should use `CleanupMode.ON_SUCCESS`.
- The user turn is persisted twice for `recordsToThread` shapes (`ConversationEngine` dispatch
  wrapper and the substrate's `threadUserMessage`).

## Round 20 — final fresh-install round on candidate 3 (pre-registered 2026-09-07)

Charter: `941-evidence/round-3-charter.md`. Mode `fresh-install`, `--no-models`, Codex
first. Candidate 3 is the unsigned `build-installer.yml` dispatch of `main` after the
round-19 fix PR merged (lineage row above). This is the runbook's final qualifying round: a
clean pass qualifies the commit for the signed tag build. Re-confirmation items with their
healthy signatures are enumerated in the charter (F1 long-running Structured probe in the same
conversation as the Documents answer, before any restart, then the fresh-conversation
control; F2 after a full periodic sync cycle; F3 `installedFully:true` with no `chat-compact`
row; F4 frame label after the genuine cold restart; round-18 fixes; H1 no false predates-boot).
Golden baseline: candidate-1 file reused (neither campaign touched search).

## Owner decision 2026-09-07 — round 20 skipped, publish from `main@59ae966dd`

The owner decided to skip the pre-registered round 20 and publish 0.3.0 in its current state.
What that means for the record: the round-19 fix campaign (R19-F1..F4, H1, PR #703) is verified
by fail-before tests, the full unit suites, the ui-web gates and the backend build, but NOT by a
Sandbox round — no fresh-install journey has run on the shipped commit, and the F1 follow-tail
mechanism has no live scroll capture (its falsifier stays in `round-3-charter.md` item 1 for the
next release's first round). The last completed round on the 0.3.0 line is round 19 on
candidate 2 (`main@6bff10e4a`); the shipped commit differs from it by #703 only. The
round-20 charter is retained as the next release's first-round charter template.

Release notes: the eight Search v3 `### Added` bullets (#533, #529, #573, #503, #505, #527,
#516, #514) were dropped from the 0.3.0 section before the cut — `core.search-v3-surface` is
still `audience: DEVELOPER` / `placement: DEEPLINK` on the shipped commit (938 §B.1), so those
features are not user-reachable. The `check-window-cutover` deadline (2026-09-30) is untouched
by this release and still needs the 852 decision.

## WinGet retired end to end (2026-09-07)

The post-cut runbook step still dispatched `prepare-winget-manifests.yml`, although the owner had
already withdrawn the upstream submission (`microsoft/winget-pkgs#429017`, 2026-09-04; 938 §B and
905 §O.4). Its two failures on 0.3.0 were fixed (#706 publishedAt formatting, #707 pinned WinGet
CLI on the runner) before the owner stated that JustSearch was never meant to be installable via
WinGet. The sweep removes the workflow, `scripts/release/winget-manifests.mjs` and its test, the
`packaging/winget/` metadata and tracked 0.2.0 manifests, the CI projection-test lines, the
`workflow-signal-policy` entry, and the three canonical-doc passages (cut-a-release,
verify-your-download, agent-guide). The `winget-manifests-v0.3.0` artifacts of runs 34099046708 and
34100579166 were deleted. Generic "winget" mentions that describe third-party tooling
(`scripts/bench/lhm-log.ps1`) or installer semantics (`sandbox-guest-silent-test.ps1`) are not
JustSearch distribution and stay. Older tempdocs (760, 887, 905, 938) keep their dated history.
